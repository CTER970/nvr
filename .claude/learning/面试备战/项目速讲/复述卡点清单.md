# 速讲复述卡点清单

> 速讲轨道交付物：每讲学生复述中暴露的卡点，当场补讲后登记；收尾拷打前过一遍。不写主线文档。

## 第 1 讲 · 电梯版与三层架构（2026-09-07，通过·带卡点）

**口述交付情况**：三层表整段复述✓；数据格式三腿方向✓（录像压缩域根约束口头确认）；30 秒电梯版二答通过（四格结构齐）。

**卡点登记**：
1. **「没有 VPU」主语归属含混**（必改级）：表述顺序导致听感像 V821 没有 VPU；实为 V821 有 VENC、IMX6ULL 无硬解。已给修正句。
2. 云返回方向说成「经过编码」→ 应为**解析**：JSON 文本→解析出字段→事件结构体→发布→报警/入库→Web 事件列表（闭环最后一环首答也漏，二答补上报警）。
3. 浏览器预览腿（MJPEG 走 HTTP）四问号中漏答。
4. 转发透传腿在 30 秒版中两次漏（主路=录像 remux+转发透传两条腿）。
5. 「裸流」升级话术：H.264 还没装容器，remux 换容器不重编码（主动省一个追问）。
6. 角色句降级版两次未交付 → **三答已交付（2026-09-07，四要素齐），第 1 讲收口**。

**通过判定**：30 秒版通过 + 角色句过闸；2 分钟版六块结构已备（讲义 01），彩排环节（第 5 讲）再整场检验。

## 第 2 讲 · 模块地图与四条链路（2026-09-07，口述进行中）

**口述交付情况**：热更五项全对✓；三模块输入输出对（均挑已学区 cloud_ai/event_bus/frame_grabber+alarm）；「C 挂 A 在」方向✓；画图以口述走图替代（速讲允许）。

**卡点登记**：
1. **转发腿第三次漏**（第 1 讲×2 + 本讲）：记忆法=说「三分发」掰三根手指——录像 remux／抽帧／转发透传。rtsp_server 是自研协议含金量话术载体。
2. **必须重启项答错**（答成 cloud_ai_reload，实为热更五项之一）：正确=源 RTSP 地址/监听端口/存储目录。⚠️「源地址不热更」=简历历史错误（×3）同族第四犯，个人高频雷，已单独重讲。
3. 「封装进 mp4 结构体」术语错：MP4 是容器/文件格式，非 C 结构体。
4. 「C 挂 A 在」机制表述虚（"数据流直接封装所以无影响"）→ 正解：独立旁路线程+取拷贝+失败不回灌（2.1 存货）。
5. D 链口述过虚（"包含 post api 等内容"）→ 改用降层话术（MJPEG 预览/Range 回放/事件+配置 API）。
6. 学生提问「旁路是否复制一份 packet」→ 已直讲：packet 纯借用零复制；clone 在 A 腿（write_frame 掏空包）；拷贝在 JPEG 交接两处（memcpy 进缓存/malloc 出口）。口径「包是借的，JPEG 才拷」。

**待补口述**：①转发腿一句 ②重启项一句 → **均已交付且超线（2026-09-07）**：三分发 c:157/164/168 行号核实全对（"一份 packet、三条腿、三种格式"口径成型）；重启项答出两层结构（web.c:825 代码自认 + 对照 conf.h 推不进五路：rtsp_url/transport/timeout、路径类、rtsp_server_port、mqtt 全家、log_file），引用全部核实属实。

**收口残留微修**（第 5 讲彩排复查）：
1. 「转发给预览客户端」措辞与源码注释 c:166 一致（无错），但面试加定语「RTSP 客户端（如 VLC）」以免与浏览器 MJPEG 预览腿混淆。
2. 「因为三个模块处理都很快速」因果要翻转：快是**前提**不是保证——同步串行的**代价**是任一消费者变慢拖住泵（慢 TCP 客户端持锁 send 是已知风险），换来零队列内存+顺序保证+天然背压。取舍话术，不是免费午餐。

**第 2 讲收口(2026-09-07)**。

## 脊梁段 1 · event_bus 函数功能层(2026-09-08,过闸)

**口述交付情况**:9/9 函数分四批交付,每函数「做什么/输入输出/出口类型」齐;③框架④why 通过。实质错误仅 1 处措辞级(AUTOINCREMENT),当场精修。

**卡点登记**:
1. 微修:「回调须快速返回」契约出处= **event_bus.h:18**(回调 typedef 的注释),口述说成「alarm.c 的注释」——引用位置错,因果无误。
2. AUTOINCREMENT 措辞精修:普通 INTEGER PRIMARY KEY 分配 id 用 max+1,**删最大行后 id 会复用**;AUTOINCREMENT 查 sqlite_sequence 记的**历史**最大,永不回退。

**超线亮点(应答卡素材,表达课复用)**:
- 「总线只传不攒——攒了才要队列,要队列才要线程」(event_bus 为什么 0 线程)
- 「延迟记在谁头上,谁才知道疼」(被动库借调用方线程)
- get 三出口同返 -1 =「三种世界」:系统故障 vs 合法的查无此件,web 侧只能糊成一个失败;规范解法 0/-1/-2 学生自答
- BLOB 反对理由两条:「高频小查询给低频大图陪葬」(行肥索引瘦)+ 从库取图=大块搬运进 g_lock 临界区,与「锁内只做快事」相撞
- NULL 语义链:SQL NULL(带类型标记的无值)≠ 空串;column_text 无字节可指只能给 C NULL;数值列 NULL→0 是 sqlite 定义的宽容转换

**通过判定**:publish 五步✓ / 查询家族真干活与转调✓ / 框架(0 线程+锁保护对象)✓ / why(BLOB vs 文件,查询+web 两角度)✓ → **段 1 过闸,主线 L1 观察级免重做**。

## 脊梁段 2 · alarm 函数功能层(2026-09-08,过闸)

**口述交付情况**:8/8 函数四批交付,**零实质错误**(两段首次全对通关);过闸两问(回调 why+订阅侧上下文)通过。

**卡点登记**:无实质卡点。口头提醒一条:c:63 空串返回 false 的「没配触发类型=安全态永不响」语义口述中未主动复述,彩排时留意别丢。

**超线亮点(应答卡素材)**:
- 「等待没有消失,只是被挪到了唯一以等待为业的线程上」(判定在回调、动作在线程;生产者发信号/消费者专职等)
- 响铃期间新事件命中 on_event 仍秒回(覆盖刷新 g_pending)——阻塞永远只在报警线程一侧
- #13「锁白上了一半」独立定位到变量粒度:on_event 只有 g_active_s 在锁内读,trigger_types/min_conf 都在 c:79 锁外
- strtok 毁副本:不拷贝则「第二个类型永远匹配不上,报警在第一个事件后静默残废」;多线程需 strtok_r
- usleep(100ms)=等内核异步建 sysfs 节点,「没有完成通知,时间换确定性,不是协议保证」
- stop 顺序三逻辑:标志先行(两循环唯一退出条件)/锁内 signal 防「查完谓词还没睡下」丢唤醒(落空无害,秒级轮询兜)/join 等死透;GPIO 复位在 join 后(交错:线程刚过 c:101 检查、你复位、它再拉高)
- 写法不对称第三例:init strncpy(c:119)不补 \0 靠 BSS 零初始化,reload(c:162)显式补——同文件对比法

**通过判定**:交接链✓ / int-not-bool✓ / 框架(1 线程对照 0 线程)✓ / 回调 why✓ / 订阅侧一句话✓ → **段 2 过闸;主线 2.3B 的 L1 压缩为核对订阅者名单(alarm.on_event)**。

## 脊梁段 3 · C 链路走查(2026-09-08,过闸)

**口述交付情况**:数据形态①–⑥+三去向首答交齐;none 过滤/同步转交判据/cloud_ai 整体框架追问后补交全对;alarm 框架主动完整复述零错。实质错误:零(一处精修)。

**卡点登记**:
1. 形态①初答只给了「AVPacket→AVFrame」,漏第二跳「AVFrame(YUV)→JPEG」——feed 站是两连跳,旁路存的那份是 **JPEG 非 AVFrame**(旁路代价=每帧完整解码+编码,不是拷指针)。当场精修。

**超线亮点(应答卡素材)**:
- 「先库后文件,不然不知道叫什么名」(快照文件名=行 id 的跨函数顺序因果)
- 「快速返回契约的来源=回调借发布者的栈跑」(契约↔执行上下文)
- 「publish 返回时库里有行、盘上有文件,但物理世界还没任何变化」(同步 vs 转交的判据)
- 方法论金句:「同步=同一栈帧链做完;转交=过了共享变量+signal 的接力点,时间轴换主」
- get_jpeg=「冻结快照/生命周期解耦」+ realloc 搬家+原地覆写(c:160-164)
- ai_thread **拉模式** vs feed 推模式;节流=「上限节流,不是精确 fps」(单次调用可耗时数秒)
- none 受害面两层:events 表暴涨拖慢 web 历史查询 + 无事 evt_*.jpg 塞目录靠 cleanup 收

**通过判定**:整线函数名+数据形态不断链✓ / 三分叉+同步转交判据✓ / cloud_ai+alarm 整体框架✓ → **段 3 过闸,脊梁 3/6;面试题「挑一条链路从进讲到落」C 链路全套就绪**。

## 脊梁段 4 · C3/C4 压缩 + web 函数功能层(2026-09-13,过闸)

**口述交付情况**:分批交付(批1 读请求+拆包/批2 分表/批3 MJPEG/引导1 五步链/批4-6 Range+events 接缝+config 三步/批7 URL 映射表降级核对/终口述 2min 全链)。实质错误:零(1 条措辞级挂账)。

**卡点登记**:
1. 措辞级(批1):「一次 recv 大概率全到,头部一个 TCP 段装得下」→ 精修:recv 返回的是**内核缓冲当前已到的字节**,不按 TCP 段对齐;正确表述=「小请求通常整坨很快到达」,「只收一次」仍是假设非保证。

**超线亮点(应答卡素材)**:
- 「只赌头不赌 body」(一次 recv 假设 vs c:900 老实补读)
- 「谁在要/要什么/带多少货」(method/path/Content-Length 三步拆包)
- 「文本靠分隔符、二进制靠长度,一条连接两种规则并存」(MJPEG 小节头 Content-Length 不可省)
- 「MJPEG 没有挂断通知,靠 send 报错自然发现」(四条退出路)
- 「无 Range 只是 Range 的特例」(默认区间 0..total-1,发送循环共用)
- 「web 不是订阅者,HTTP 一问一答走拉不走推;同一条 event_bus,alarm 推/web 拉,两副面孔」
- 「出口打了码,入口就得认得码、放行不进货」(**** 特判 c:775/805)
- 副本三步防两种坏状态:失败原子性(内存新文件旧)+并发安全(读者拿新旧混合体)
- 「三套防御:长度必夹、外来数据必洗、动态内存必验」(serve_events)

**通过判定**:收拆分发五步链✓ / 分表 GET 九路+POST 一路+405✓ / 四代表 4/4✓ / D 接 C 缝(query_filtered 调用方出容器)✓ / 终口述全链行号零错✓ → **段 4 过闸,脊梁 4/6;C3/C4=机制已声明(主线 3.1/3.4 仍需补套接字生命周期)**。

## 脊梁段 5 · conf 函数功能层(2026-09-13,过闸,压缩版)

**口述交付情况**:压缩方案执行(过码砍至三小段);三入口一句话+热更五路/重启项两表交齐;生效时机抓得准。

**卡点登记**:
1. **token 列入热更全家(实质,旧错复发)**:段 3 已过闸结论「token 不可热改」本次复发。cloud_ai_reload 白名单六字段(c:355-363)无 token,c:354 注释明说保留原值;改 token=落盘+重启生效。复测候选。
2. **MQTT 重启理由编造**:答「客户端连接启动期建」——连接不存在,conf.h:52「预留,仅存取,未接入客户端」;归类应是「存而不用」,重启也不生效。
3. 措辞级两条:conf_load 产出=调用方递入的结构体(main.c:117),conf.c 无全局配置副本;storage_dir 热不了=模块持有启动 cfg 副本,非「句柄一直开」。

**超线亮点(应答卡素材)**:
- 「报警是逐事件判断,每次拿新参数判」(alarm_reload 语义)
- 「下一段切文件时才用到的数值」(segment_seconds 生效时机)
- 「改内存里的数字换不回已绑的端口」(监听类为什么热不了)
- 教练补的对照:两个 fps 两种命运——frame_grabber 每帧现读(真生效,c:138-139)vs cloud_ai interval_us 循环外冻结(半生效,c:302);一句话答「改了 AI 帧率为什么感觉没变化」

**通过判定**:三入口✓ / 五路推送✓(token 纠偏)/ 重启项框架✓(MQTT 纠偏)→ **段 5 过闸,脊梁 5/6;D 链路只欠段 6 拼装**。

## 脊梁段 6 · D 链路+2.5 口径+app.js(2026-09-13,过闸,脊梁收官)

**口述交付情况**:主口述 10 站形态链零实质错误(编号笔误 1 处:⑧误写③);「无推送、拉模式」架构特征主动点出且精确(仅发布→报警是推);副口述配置链四步一字不差,自带两处自纠。

**卡点登记**:无实质卡点;编号笔误 1 处。

**超线亮点(应答卡素材)**:
- 「压缩包→原始帧→图片→文本→请求→字节流→结构体→数据库行加文件→响应→页面」(一句话数据形态总链,30 秒版开场可直接用)
- 「全程只有发布到报警是推,web 侧从头到尾是拉」(C 写 D 读无推送,衔接它们的只有库和盘)
- 自纠:app.js 两条前端路(481 开关即时单字段/517+663 表单整包)汇到同一 /api/config——「三入口」素材别讲混
- 自纠:「热更五路立即生效,重启项落盘待重启」(收窄尾句,引段 5 结论)

**通过判定**:10 站形态+函数不断链✓ / 拉模式特征✓ / 配置链+自纠✓ → **段 6 过闸;框架脊梁(段 1-6)全部收口:四条链路 A/B/C/D 全部到函数功能级,「挑一条链路从进讲到落」四选一全部成稿**。

## 表达课段 7 · 我做了什么(2026-09-14,过闸)

**交付情况**:五条人话版全交付(`04_人话版口述稿.md`),①2min+8子弹/②60s+4/③60s+4/④60s+5/⑤90s+5;④⑤自带边界声明;另交背压概念块+字节纸笔卡+口径修正清单。缺陷故事按用户要求挪段 8。角色句第 1 讲已过闸不重测。

**卡点登记**:
1. **段首丢包场景限定错(最重要)**:「段首等关键帧期间非关键包主动扔」只适用**首段启动/开段失败重试**;**正常业务切段不丢包**(到时未遇关键帧继续写旧段,段长略超)。两笔丢的第一笔必须带场景限定。
2. fail_count「三次失败就踢」→ **连续失败超 3 次(第 4 次)才踢**(rtsp_server.c:322)。
3. 「注释自认待重标定」——**09-14 段8 复核更正**:recorder.c **有**此注释(c:236),系学生注释工程化时本人改写(原仓库该行写「输出用源流时基 1/90000」,把假定当事实;段7 核查只查了 c:256,漏 236)。口头口径不变=审计归属自己,可加「顺手把误导注释改成如实描述」这层亮点。
4. 双保证归因两轮未吸收:锁防撕裂(拷贝中)/拷防悬垂(拷贝后),已写入④②精修表#3。
5. 口头报配置数字(5fps)→「目标帧率」措辞。
6. 「全链站在丢这边」→ 录像腿例外(阻塞+熔断重连)。
7. 措辞级:「下一段生效」是日志原话非注释(recorder.c:206);M=双条件;「C 没有现成 refcount」→「不值」非「不能」。

**超线亮点(应答卡素材)**:
- 「两个快照别混」:分析前在内存会覆盖/分析后在磁盘挂证据按 id 命名
- 「一借一克隆一借」+「包是借的,JPEG 才拷」
- 「解码为了对,节流为了省,一个全量一个挑着来」
- 「用丢旧保新换延迟有界」/「延迟叠加的前提是有地方无限制攒东西」
- 背压判断一句话:「消费者变慢后,生产者有没有跟着慢」+同一腿两路线(TCP 阻塞=坏背压/UDP EAGAIN=丢弃)+流控六路菜单+「压力去哪」金句
- 「冻结=时间上冻结在这一刻,空间上和仓库脱钩」
- 「慢的代价从延迟变成漏看,漏看不伤实时性」(失败影响粒度自纠)
- token「半套凭证」+错配窗口 401 实体:「调参数和换钥匙,从来是两件事」
- 热更「三命运」分类:现读真热/算死半热/凭证不来(自我升级,教练仅补 enable 第四通路)
- ④「三腿两出口映射表」+「文本靠分隔符、二进制靠长度,一条 TCP 全齐」

**通过判定**:五条人话版全交付✓ / ④⑤边界声明✓ / 自我纠偏四坑(token 定案销段 5 复测候选)✓ / 段前核查推翻「转发 ts 恒 0」并完成讲义/记忆三处修正✓ → **段 7 过闸(表达课 1/3);2 分钟完整版串讲留段 9 彩排**。

## 表达课段 8 · 追问应答卡(2026-09-14,过闸)

**交付情况**(双线,与 05 讲义互补):①正式讲义线=`05_应答卡.md` 11 卡(A 通用 5+B 横切 3+C 缺陷 3)+短板话术,讲解已发,**其口述检验留段 9 彩排**;②实战演练线(本会话)= 简历五条**内部**第一问 A 组 8 条全交付,每条 30 秒级完整应答,自引行号 4 处全核实(frame_grabber c:163/c:190-203、web c:189-197/c:418-427)。**#52 在表达层自愈**:卡 3 口述「每帧都解、出图挑帧、间隔到了才转 JPEG」顺序零复发(段 7 刚登记的复发点,销账)。

**用户节奏决策**:身份/边界类(热更边界/角色句/板端闸门/缺陷题)演练推迟到主线收尾后(提醒在档:闸门降级话术系主线未完成期专用);三缺陷故事不单独演练、留段 9 彩排整场检验(与 05 C 组口径一致);缺陷深挖/修复实验=回补池待主线完成。

**卡点登记**:
1. **Range 金句挪位(实质)**:「无 Range=Range 特例」说的是 serve_file 回放路径(:396 默认区间/:399 bytes= 判定/:423 else 200/:434 lseek 共用),不是 MJPEG 直播;定稿口径=「MJPEG 无 Range 是寻址无意义;/rec 才是 Range 主场,无 Range 头在代码里就是 bytes=0-」。学生自纠重说+行号自查全对,已入 04 附块 C #10。
2. **「长 GOP 结构」概念反**:GOP 越长参考链越长,是跳帧敌人;正确三档=子码流(工程标准,本项目真实修法)/SVC 时间分层(Annex G,T0 自含)/全 I 帧(GOP=1);反例=intra refresh 无干净切入点、closed GOP 只解决起点不解决跳中段。口径「跳解合法性来自参考链闭包,客户端单方面跳包做不到」。
3. **⑨ 修法归属**:学生「最小运行容量+切段检测」=预防层(cleanup_disk 已有同思想);缺陷本体=return 0 身兼两职致计数清零(recorder.c:216 → stream_receiver.c:160),主修法=区分返回值(与 05 C2 一致);水位保护只能当加分句。
4. 精修:「解码负载掉一个量级」=预期非实测,按闸门不报数字。

**超线亮点(应答卡素材)**:
- **Range 非标准两处**(「和标准一致吗」的诚实答案,05 未收):unsatisfiable 区间静默钳回全文件不回 416(web.c:406);后缀区间 bytes=-500 未实现——strtol 读出负数被钳回 0 退化全文件(c:401+406)
- 「无 Range=bytes=0- 特例」的代码结构体现:同一对 start/end 变量+同一 lseek 循环,普通 GET 只是取默认区间
- 「云是增强,不是依赖」「慢的代价从延迟变成漏看,漏看不伤实时性」首答即出
- 「各自拉」反例的完整代价面:三份带宽+三套 demux+三个连接各自重连+三会话时序对不齐

**通过判定**:简历五条第一问 8 条全过(2 实质纠偏吸收确认)→ **段 8 过闸(表达课 2/3);05 卡口述+三缺陷检验留段 9 彩排;缺陷深挖/修复=回补池待主线(用户标记)**。下一段=段 9 彩排→拷打简历。
