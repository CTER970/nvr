# 速讲过码台账

> 本轨道唯一学习痕迹(另有 `复述卡点清单.md`)。主线 tutor 口令「继续 2.3A」开场**必读**本文件。
>
> **折算**:某行「函数功能层过闸」= 该模块主线 L1 观察级(阅读清单+能指认输入输出/主流程)免重做。不免 L2 失败路径、审计深挖、闭卷/变式。不写掌握档案,不推进任务 ID。
>
> 状态:未开始 / 讲解已发 / 过码中 / **过闸**。

更新:2026-09-08(段 1/2/3 过闸)

| 段 | 模块/课 | 已扫函数 | 能串的主流程 | 明确没碰(留主线) | 状态 |
| --- | --- | --- | --- | --- | --- |
| 1 | event_bus(2.3A 函数功能层) | 9/9:subscribe(c:32)/init(c:72)/publish(c:110)/row_to_event(c:183)/query(c:197)/count(c:203)/query_filtered(c:225)/get(c:262)/deinit(c:284);错误分支均标出口类型,叶子(cleanup_snapshots)看名 | publish 五步:门外验戳→锁内 INSERT 四步曲+rowid 回填→快照 evt_<id>.jpg+UPDATE 回填路径+cleanup→copy-then-release→锁外同步回调;查询家族读侧(query_filtered 真干活/query 转调/count/get/row_to_event 翻译);框架=0 线程被动库,g_lock 串行化 g_db 连接+保护 INSERT→last_insert_rowid 读改写;已核对调用方上下文(cloud_ai.c:277 写/web.c:256/474/516/547 读/main.c:140 init) | 审计①快照 I/O 持锁深挖(含 step 亦锁内写事务);UPDATE c:158 未检查返回值;deinit 与并发 publish/query 竞态;subscribe 满 8 静默(学生已自察 void 无感知);get 三出口同返 -1 的 404/500 语义分层(学生已自察);SQLITE_TRANSIENT 细节;生命周期 | **过闸(2026-09-08)** |
| 2 | alarm(2.3B 订阅侧并入) | 8/8:on_event(c:76)/alarm_thread(c:89)/init(c:114)/stop(c:140)/reload(c:155)/type_matches(c:61)/叶子 sysfs_write(c:33)/gpio_set(c:42) | on_event→alarm_thread 交接链:命中判定(type_matches+conf 达标)→锁内置 g_pending(=active_s,缺省兜 3s)+signal→秒回;线程 cond_wait 醒→锁内取件清零→GPIO 拉高→逐秒 sleep(可中断)→拉低;stop 三步(标志→锁内 signal→join)+GPIO 复位在 join 后;框架=1 线程 1 锁 1 cond 1 int,对照 event_bus 0 线程;订阅侧=alarm.c:129 挂 g_subs_cb,c:177 在 publish 线程(cloud_ai)上下文执行 | GPIO 失败路径(注:alarm.c 实无邮件通道,00 表"邮件"系笔误,以源码为准);#13 竞态深挖(学生已定位变量粒度:g_active_s 锁内读 vs trigger_types/min_conf 锁外读);init strncpy 不补 \0 依赖 BSS 零初始化;strtok_r 多线程边界;stop 与 reload 并发窗口 | **过闸(2026-09-08,零实质错误)** |
| 3 | C 链路走查 | —(联系课,依托段1/2+主线2.1/2.2,无新过码) | 全链串讲:feed(H.264→YUV→JPEG 两连跳,只存最新)→get_jpeg(malloc 冻结快照,拉模式)→ai_thread 四步循环(取帧/process_one_frame 五段漏斗/free/上限节流)→b64→JSON body→HTTPS→JSON 文本→event_t→滤 none→publish 三分发(库行+evt_<id>.jpg 快照+on_event 借发布者栈)→g_pending→alarm_thread→GPIO 电平;同步/转交判据=调用栈归属+「共享变量+signal」接力结构;cloud_ai 整体框架复述(1线程/主循环四步/上下游/热启停 reload 含 web 线程 join 安全);alarm 框架复述零错 | 2.5 主线闭卷(D 侧留段6 口径);失败矩阵;call_cloud 停靠时序细节;审计③ stop 时长 | **过闸(2026-09-08)** |
| 4 | C3/C4 压缩 + web | — | handle_client_inner 分发 + 四代表 | 套接字生命周期;Range 规范;全 serve_* 实现 | 未开始 |
| 5 | conf | — | conf_load;conf_apply_hot 推给谁 | 热更新不生效类审计;conf_save 失败路径 | 未开始 |
| 6 | D 链路+2.5 口径+app.js URL 映射 | — | 一张图→Web 事件列表 | 主线 2.5/3.x 闭卷 | 未开始 |
| — | rtsp_server 实现层 | 本课表不读 | 全局图:三分发第三腿,透传不解码 | parse_nalus 边界/会话/持锁 send | 移出脊梁,回补池 |

## 主线恢复时怎么用

1. 读本表,过闸段的 L1 免重做,从「明确没碰」列接深挖。
2. 段 1 过闸后:「继续 2.3A」= 跳过 L1 三问与阅读清单,从失败路径/审计①接;学生未答过的原 L1 三问作废,不补做。
3. 段 2 过闸后:2.3B 的 L1 压缩为核对订阅者名单(alarm.on_event),不重开 80min。
4. 段 4 C3/C4 标「机制已声明」后:主线 3.1 开场 C4 可压到核对,仍要补套接字生命周期。
