# 速讲过码台账

> 本轨道唯一学习痕迹(另有 `复述卡点清单.md`)。主线 tutor 口令「继续 2.3A」开场**必读**本文件。
>
> **折算**:某行「函数功能层过闸」= 该模块主线 L1 观察级(阅读清单+能指认输入输出/主流程)免重做。不免 L2 失败路径、审计深挖、闭卷/变式。不写掌握档案,不推进任务 ID。
>
> 状态:未开始 / 讲解已发 / 过码中 / **过闸**。

更新:2026-09-14(段 7 过闸,表达课 1/3)

## 进度总览(2026-09-14)

| 段 | 内容 | 预算(时间盒) | 状态 | 实际用时 |
| --- | --- | --- | --- | --- |
| 1 | event_bus 函数功能层 | 45–60min | ✅ 过闸(09-08) | 未报告 |
| 2 | alarm(2.3B 订阅侧并入) | 45min | ✅ 过闸(09-08,零实质错误) | 未报告 |
| 3 | C 链路走查 | 20–30min | ✅ 过闸(09-08) | 未报告 |
| 4 | C3/C4 压缩 + web | 90–105min | ✅ 过闸(09-13,口述零实质错误) | 未报告 |
| 5 | conf 三步(压缩版) | 45–60min | ✅ 过闸(09-13,纠偏 2 处) | 未报告 |
| 6 | D 链路+2.5 口径+app.js 映射 | 30–40min | ✅ 过闸(09-13,零实质错误) | 未报告 |
| 7 | 第 3 讲·我做了什么(讲义 03) | 40min | ✅ 过闸(09-14,五条人话版+边界声明) | 未报告 |
| 8 | 第 4 讲·追问应答卡约 10 条 | 45–60min | **待办(下一段;缺陷故事已从段 7 挪入)** | — |
| 9 | 第 5 讲·彩排 → 拷打简历 | 60–90min | 待办 | — |

**框架脊梁 6/6 收口**,含表达课 6/9;主线暂停于 2.3A L1 待答,恢复口令「继续 2.3A」。折算规则见下方主表引言与「主线恢复时怎么用」。

| 段 | 模块/课 | 已扫函数 | 能串的主流程 | 明确没碰(留主线) | 状态 |
| --- | --- | --- | --- | --- | --- |
| 1 | event_bus(2.3A 函数功能层) | 9/9:subscribe(c:32)/init(c:72)/publish(c:110)/row_to_event(c:183)/query(c:197)/count(c:203)/query_filtered(c:225)/get(c:262)/deinit(c:284);错误分支均标出口类型,叶子(cleanup_snapshots)看名 | publish 五步:门外验戳→锁内 INSERT 四步曲+rowid 回填→快照 evt_<id>.jpg+UPDATE 回填路径+cleanup→copy-then-release→锁外同步回调;查询家族读侧(query_filtered 真干活/query 转调/count/get/row_to_event 翻译);框架=0 线程被动库,g_lock 串行化 g_db 连接+保护 INSERT→last_insert_rowid 读改写;已核对调用方上下文(cloud_ai.c:277 写/web.c:256/474/516/547 读/main.c:140 init) | 审计①快照 I/O 持锁深挖(含 step 亦锁内写事务);UPDATE c:158 未检查返回值;deinit 与并发 publish/query 竞态;subscribe 满 8 静默(学生已自察 void 无感知);get 三出口同返 -1 的 404/500 语义分层(学生已自察);SQLITE_TRANSIENT 细节;生命周期 | **过闸(2026-09-08)** |
| 2 | alarm(2.3B 订阅侧并入) | 8/8:on_event(c:76)/alarm_thread(c:89)/init(c:114)/stop(c:140)/reload(c:155)/type_matches(c:61)/叶子 sysfs_write(c:33)/gpio_set(c:42) | on_event→alarm_thread 交接链:命中判定(type_matches+conf 达标)→锁内置 g_pending(=active_s,缺省兜 3s)+signal→秒回;线程 cond_wait 醒→锁内取件清零→GPIO 拉高→逐秒 sleep(可中断)→拉低;stop 三步(标志→锁内 signal→join)+GPIO 复位在 join 后;框架=1 线程 1 锁 1 cond 1 int,对照 event_bus 0 线程;订阅侧=alarm.c:129 挂 g_subs_cb,c:177 在 publish 线程(cloud_ai)上下文执行 | GPIO 失败路径(注:alarm.c 实无邮件通道,00 表"邮件"系笔误,以源码为准);#13 竞态深挖(学生已定位变量粒度:g_active_s 锁内读 vs trigger_types/min_conf 锁外读);init strncpy 不补 \0 依赖 BSS 零初始化;strtok_r 多线程边界;stop 与 reload 并发窗口 | **过闸(2026-09-08,零实质错误)** |
| 3 | C 链路走查 | —(联系课,依托段1/2+主线2.1/2.2,无新过码) | 全链串讲:feed(H.264→YUV→JPEG 两连跳,只存最新)→get_jpeg(malloc 冻结快照,拉模式)→ai_thread 四步循环(取帧/process_one_frame 五段漏斗/free/上限节流)→b64→JSON body→HTTPS→JSON 文本→event_t→滤 none→publish 三分发(库行+evt_<id>.jpg 快照+on_event 借发布者栈)→g_pending→alarm_thread→GPIO 电平;同步/转交判据=调用栈归属+「共享变量+signal」接力结构;cloud_ai 整体框架复述(1线程/主循环四步/上下游/热启停 reload 含 web 线程 join 安全);alarm 框架复述零错 | 2.5 主线闭卷(D 侧留段6 口径);失败矩阵;call_cloud 停靠时序细节;审计③ stop 时长 | **过闸(2026-09-08)** |
| 4 | C3/C4 压缩 + web | C3/C4 三声明(文本协议四段式/这条连接 cfd/MJPEG=同连接连推 JPEG);web:入口 web_start(c:1003)/web_stop(c:1021);organizer 链 web_thread(c:964)→client_thread(c:955)→**handle_client_inner(c:862)**:一次 recv(4095 赌头)+sscanf 请求行+req_hdr_get 发货单+strstr 定体补读+分发(GET 九路/POST 一路/405);四代表精读 serve_mjpeg(c:185)/serve_file(c:380:206+Content-Range+段长,lseek 共用)/serve_events(c:450)/serve_config_post(c:732:副本叠加→save→apply_hot→提交);其余 7 个 serve_* 建 11 端点 URL 映射(tutor 交付);叶子看名 | 浏览器 GET /api/events?limit=20 全链 2min 口述:accept(c:988)→client_thread→收拆分发→serve_events 终审递箱(evs[64]+20+NULL+0)→query_filtered 锁内 prepare(2 个 ?)/bind/step×row_to_event(c:183)/finalize 解锁→回拼 JSON(escape/折文件名/realloc 翻倍)→send_json(c:128)/send_http(c:104)→close(cfd);框架=1 web_thread+每连接 detached client_thread,MJPEG 长连接占线程;g_cfg=POST 叠加基底(c:745);web 对 event_bus=拉模式非订阅者,与 alarm 推模式互为镜像 | 套接字生命周期(shutdown 唤醒 accept/两种 fd 生灭/异步启动);Range 规范边界;全 serve_* 实现细节;C3/C4=机制已声明未过码 | **过闸(2026-09-13,口述零实质错误)** |
| 5 | conf | 三入口+format_value 白名单+叶子看名:conf_load(c:55) 三层(memset 清零→硬编码默认值 c:60-87→文件覆盖 40 键 strcmp 阶梯,缺键保默认,未知键静默);conf_save(c:243) 逐行替换保注释+.tmp→fsync→rename 原子顶替;conf_apply_hot(c:296) 五路推送;format_value(c:197) 41 键镜像(注释称敏感字段不在白名单,实然在,以源码为准) | 热更五路 vs 重启项口述:log_set_level(改比较阈值)/alarm_reload(锁内整包,逐事件判断)/cloud_ai_reload(六字段白名单 c:355-363,**token 保留不可热改**)/frame_grabber_set_target_fps(web_mjpeg_fps=进货节流,feed 每帧现读 c:138-139 真生效)/stream_receiver_set_segment_seconds(切段时才用);重启项=监听类(bind 死)/拉流源类/路径类(模块持有启动副本)/rtsp_server | conf_save 失败路径;审计①深挖(cloud_ai.c:302 interval_us 循环外冻结,ai_fps 热更半生效——vs frame_grabber 每帧现读,同项目两写法对照);掩码对称性;alarm #13 族 | **过闸(2026-09-13,纠偏 2 处:token 复发+MQTT 理由)** |
| 6 | D 链路+2.5 口径+app.js URL 映射 | app.js 映射表 10 动作核对(状态轮询 c:80/预览 c:151·180/抓拍 c:255/录像 c:329/回放 c:356·367/事件列表 c:416/缩略图 c:430/配置读 c:463/写双入口 c:481·663);实然:/api/event/<id> 前端未接,系预留端点 | 主口述 10 站形态链零错:packet→YUV→JPEG(两连跳+节流入库)→malloc 冻结快照→base64/JSON 请求体→HTTPS 往返→event_t(滤 none)→INSERT+编号命名快照+UPDATE 回填双写→拉模式查询(serve_events)→DOM+缩略图二次请求闭环;「全程仅发布→报警是推,web 从头到尾是拉」;配置链副口述四步一字不差+自纠两处(app.js 双入口=481 开关单字段 vs 517+663 表单整包;「热更五路立即生效,重启项落盘待重启」收窄尾句) | 主线 2.5/3.x 闭卷 | **过闸(2026-09-13,零实质错误;脊梁 6/6 收口)** |
| 7 | 第 3 讲·我做了什么(表达课) | 讲义 03 口径+段前核查;产出 `04_人话版口述稿.md`:五条人话版全交付(①主口述 2min+8 子弹/②60s+4/③60s+4/④60s+5 含边界声明/⑤90s+5 含边界声明)+附块 A 背压与丢弃(判断一句话/同腿两路线/流控六路菜单)+附块 B 字节纸笔卡(fMP4 布局/NALU 头/RTP 头/FU-A)+附块 C 口径修正清单 9 条+统一精修对照表 11 条;**段前核查推翻「转发 ts 恒 0」**(rtsp_server.c:296-300 已实现 pts 换算+调用点 c:168 传 tb,讲义 03 缺陷表撤④换⑨=I-06,简历口述版【角色】句待主线改) | 五条全过闸依据=①②③ L3 存货全激活+④⑤降级口径带边界声明(用户自评把握感下降→边界感方法论替代)+自我纠偏四坑(token 白名单定案销复测候选/失败粒度=丢覆盖非丢帧/分段时长下一段生效/热更三命运);角色句第 1 讲已过闸不重测 | 三条缺陷「发现→定位→修法」挪段 8 应答卡;2 分钟完整版串讲留段 9 彩排;板端闸门未开(降级口径) | **过闸(2026-09-14;表达课 1/3)** |
| — | rtsp_server 实现层 | 本课表不读 | 全局图:三分发第三腿,透传不解码 | parse_nalus 边界/会话/持锁 send | 移出脊梁,回补池 |

## 主线恢复时怎么用

1. 读本表,过闸段的 L1 免重做,从「明确没碰」列接深挖。
2. 段 1 过闸后:「继续 2.3A」= 跳过 L1 三问与阅读清单,从失败路径/审计①接;学生未答过的原 L1 三问作废,不补做。
3. 段 2 过闸后:2.3B 的 L1 压缩为核对订阅者名单(alarm.on_event),不重开 80min。
4. 段 4 C3/C4 标「机制已声明」后:主线 3.1 开场 C4 可压到核对,仍要补套接字生命周期。
