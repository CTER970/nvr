# 第 3 讲 · 我做了什么(含板端降级闸门)

> 时间盒 25–30min。事实源:`简历.md` v5 五条 + 「投递前自查」、`01_项目骨架.md` 缺陷清单。对外编号用骨架那套;18 项 TODO 只作内部定位,不在口述里报「第 15 项」。

## 硬闸门(当前默认:未开)

板端全功能并行跑通 + D1–D6 采集完成之前:

| 简历原句 | 口述必须改成 |
| --- | --- |
| 完成……部署联调 | 完成源码级梳理;板端全功能联调还在进行 |
| 分析并验证「主通路压缩域……」 | 分析「主通路压缩域……」的分工,机制有源码证据;「验证」等板端数字 |
| 任何 CPU% / 帧率 / 重连耗时 | **不报**。被问到就说「还没上板采,我可以讲测量方法和预期方向」 |

闸门打开后,才允许用简历 v5 原文。教练开场先确认闸门状态,默认未开。

## 角色句(降级版,先背)

项目是开源 NVR,不是我从零写的。我做的是源码级梳理(含原项目自研的 RTSP/HTTP)、函数注释工程化、以及一份可复现的问题清单。架构选型是原作者的,我能讲清 why 和边界;「自研」不贴在自己身上。

## 五条怎么扩(各约 30 秒,机制有存货、数字没有)

① **拉流三分发**:一次读包同步扇出到录像/抽帧/转发,避免多会话重复拉或全解码。Phase 1 L3。
② **旁路软解**:主路压缩域,JPEG 只留最新帧+拷贝交权。Phase 1 L3。
③ **remux + fMP4**:不转码;关键帧切段;提高掉电可恢复 ≠ 零丢失。Phase 1 L3。
④ **双出口**:RTSP 透传(概念层已有)+ HTTP MJPEG/Range(职责层,实现未学,追问到边界就停)。
⑤ **云事件 + 热更新五项**:失败不传染主链;热更=日志/报警/AI/MJPEG 帧率/分段时长,源地址不热更。

## 对外缺陷清单(骨架编号,挑 3 条讲「发现→定位→一句话修法」)

优先这三条(有独立发现故事、面试好讲;2026-09-13 段前核查修正):

| 对外# | 现象 | 定位 | 一句话修法 |
| --- | --- | --- | --- |
| ⑥ | Web 当前段码率恒 0 | `recorder.c:260` 在 `write_frame` 掏空 packet 后读 `out->size` | 在 write 之前记下 size |
| ⑨ | 磁盘满后录像停摆、每关键帧重试开段,不触发「写错误过多重连」 | 开段/写帧失败 `err_count++`,但下一个非关键帧走等关键帧分支 `return 0`,调用点(`stream_receiver.c:157-161`)当成功清零计数,20 次凑不齐 | 等待路径返回值与「成功」区分,不让它清零失败计数 |
| ⑦ | 录像时基写死 1/90000 | `recorder.c:256` 没接收源流时基(rtsp 腿 `c:168` 传了 tb,recorder 没接) | 把 `AVRational` 传入 recorder |

> **2026-09-13 更正**:原主选「④转发 RTP 时间戳恒 0」**撤下**——当前源码 `rtsp_server.c:296-300` 已实现 pts 按 90kHz 换算,调用点 `stream_receiver.c:168` 传入 time_base;仅无 pts 兜底分支填 0(c:302)。骨架④原始表述「**无 PTS 时**时间戳恒 0」仍成立,但正常拉流 pkt 有 pts,实际 ts 正常,当缺陷讲会在复现时穿帮。替补=骨架⑨(I-06)。**简历口述版【角色】句「转发时间戳恒 0」须同步更正**(建议改「码率统计恒 0」;简历.md 归主线/用户改,速讲侧只读)。教训=缺陷故事上口前必须对源码复核。

备用(被问「还有吗」再加,不要一次倒完):I-05 非关键帧起段、get_jpeg 解锁后重读 size、avcC 偏移差一位(`c:72-73`,09-13 复核仍在)、token 掩码回写。内部 18 项对照见 `00_速讲轨道说明.md` 附录(其中第 11 条「rtsp 转发路 ts 恒 0」同此更正,已不成立)。

## 学生本讲要口述的

1. 角色句(降级版)不看稿
2. 五条各一句,④⑤遇到实现细节停在职责层
3. 三条缺陷按「发现→定位→修法」讲,不报未测数字
