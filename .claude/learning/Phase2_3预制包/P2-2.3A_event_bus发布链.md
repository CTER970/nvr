# P2-2.3A · event_bus 发布链精读（35–40min，L3）

> 原 2.3 前半。时间盒 10min 入库顺序 + 10min 失败矩阵 + 5min 锁内 I/O 一句 + 5min 闭卷
> 前置：C1 + 2.2 + **C2 刚过**（C2 本卡不落地，落地在 2.3B）。
> 修订：2026-09-02——C1 已覆盖的回调段**只复收口一句**，禁止再讲一遍「锁内拷数组解锁调用」。

## 0. 开块审查

`grep -n "event_bus_publish\|sqlite3_prepare\|cbs\[i\]" event_bus.c`（110-180）。快照目录 main.c:136-137。

### 阅读清单（先读后问）

| 段 | 位置 | 本遍看什么 |
| --- | --- | --- |
| 入口+INSERT | `event_bus.c:112-142` | 失败返回 -1；**last_insert_rowid → ev->id** |
| 快照+UPDATE | c:144-167 | 用 id 命名文件再回填路径；fopen/fwrite **在锁内**先标位置 |
| 拷数组+解锁+回调 | c:169-178 | **C1 已学**：只确认行号，不重讲因果 |

忽略：row_to_event / query_*（2.3B）；SQLite 每条 bind 类型。

**C1 已学、本卡不重复**：回调跑在发布者线程、锁内拷贝解锁后调用、ev 指针不能存到以后用。学生能答「c:174 解锁、c:176 才调」即过。

## 1. 任务目的（四层）

① event_bus 是链路 C 的「心脏」：一次 publish 串起入库、快照、广播三件事，锁策略是全项目最精的一处——C1 学的「锁内拷贝解锁回调」在此落到逐行。② 项目背景：这是作者自研的迷你「持久化+观察者」中间层，也是 NVR 事件可追溯（SQLite 证据链）的落点；读透它=能讲「为什么不用消息队列」。③ 岗位背景：「回调与锁」「临界区里该放什么」直接对应面试「锁粒度」题；SQLite 最小 CRUD 也是嵌入式存储常客。④ 学完交 publish 分段图，2.3B 查询支路只是它的镜像。

## 2. 数据流坐标

链路 C 中枢：`AI线程栈上 event_t+JPEG字节 → publish → [锁内:INSERT→id 回填→写快照→UPDATE 回填路径→清理旧快照] → [锁内拷订阅数组] → [解锁] → [逐个同步回调] → 返回 id`。

## 3. 锚点与分段（已实测，publish 110-180）

| 段 | 行号 | 动作 |
| --- | --- | --- |
| 入口校验+时间戳 | 112-115 | !g_db→-1；id=-1；created_at=time(NULL) |
| 锁内 INSERT | 117-142 | prepare→bind×6→step→finalize；失败解锁返回 -1；**last_insert_rowid→ev->id 回填** |
| 锁内快照 | 144-167 | id 已知才命名 evt_<id>.jpg→fopen/fwrite→UPDATE jpeg_path→cleanup_snapshots |
| 锁内拷订阅 | 169-173 | cbs[8]/users[8] 栈数组快照 |
| 解锁 | 174 | **回调前**释放 g_lock |
| 同步回调 | 176-178 | 逐个 cbs[i](ev, users[i])——发布者线程上下文 |

## 4. 卡内前置（铁律 21/22）

- **SQLite**（迷你三件套，L1）：库=单文件；表=行列结构；句柄=sqlite3*。五步套路 `prepare→bind→step→finalize`（预编译防注入）；`SQLITE_DONE`=写语句完成、`SQLITE_ROW`=查到一行。
- 词源：`AUTOINCREMENT`（自动递增主键）；`rowid/last_insert_rowid`（刚插入行的 id）；`TRANSIENT`（bind 时让 SQLite 拷一份数据而非引用调用方内存）。

## 5. 引导问题（手册精选+C1 落地）

1. 事件 id、快照文件名、jpeg_path 按什么顺序产生？（先 INSERT 拿 id→再用 id 命名文件→UPDATE 回填路径——为什么文件名不能先生成？）
2. （C1 复收口，能答就过、不展开）回调执行时 g_lock 释放了吗？证据行？（c:174 vs 176-178）
3. DB 成功、快照失败时：回调还发生吗？Web 列表显示什么？（照常回调；列表有事件但 snapshot 为空）
4. 快照 I/O 在锁内，慢 TF 卡上 GET /api/events 会怎样？（**审计主菜①，一句话**。修法留动手池）

## 6. 产出与通过标准

publish 四段时序图（锁区间要明确画出）、事件三字段生成顺序表、发布结果矩阵（DB败/快照败/订阅空）。通过=三字段顺序因果 + DB 成/快照败时回调仍发生 + 锁内慢 I/O 一句话。回调/锁设计以 C1 证据为准，本卡不重复验收。

## 7. 闭卷与变式（已过深度滤网）

- 闭卷：默写 publish 骨架（含锁的起止行号量级）。
- V1（订阅空）：publish 时订阅表为空会怎样？（入库但无人响应 ≠ 通知了没持久化）
- V2（审计主菜①）：见引导问题 6，一句话即可。把快照挪锁外的改造题进提升池。

## 8. 预制时审计发现（核实后用）

①快照 I/O+cleanup 全程持锁（c:145-167）——拖 web 查询与其它发布者。②c:115 `if (ev->jpeg_path[0]==0) ev->jpeg_path[0]=0;` 无操作疑似残留（「注释/语句不是证据」小例）。③c:181-194 row_to_event 用 strncpy 不补 \0？——memset 先清零（c:185）+ strncpy(size-1) 安全，可让学生自查防呆。

## 9. 快问快答

1. publish 返回 -1 的两种情形？（未 init / INSERT 失败——快照失败不返回错误）
2. ev->id 在函数入口为什么先置 -1？（失败时调用方能识别「未入库」）
3. 两个发布者并发 publish 会怎样？（串行化在 g_lock 上——单写者简化）
4. 快照为什么用触发帧而不是重新抓帧？（证据一致性：报警画面=分析画面）
5. MAX_SUBS=8 溢出会怎样？（静默不注册——订阅丢失无告警，一句审计）

## 10. 本阶段说明草稿

链路 C 中枢；解决「事件的可追溯持久化+运行时广播」二合一；上承 2.2 的 event_t 与 JPEG（快照=触发帧证据），下启 2.3B 订阅者（alarm）与查询者（web）；面试素材：锁内做/不做慢 I/O、回调与锁的配对设计、自研总线 vs 消息队列选型。
