# Phase 2/3 前置概念框架（开卡前概念层课程地图）

> 建立：2026-08-22；**修订 2026-09-02**（C1 实战：C 块改由 nvr-tutor 教；C2 后置；C3/C4 并入源码卡开场）
> 定位：Phase 2/3 的**机制概念**，不再是「四块全部开卡前批量上完」。
> 执行者：**nvr-tutor**（阅读清单 + L1→L3 阶梯 + 必要纸笔）。禁止 knowledge-tutor 的字节样例六阶段。
> 八股出口：C1/C2 → `八股卡片/06_云AI与事件.md`；C3/C4 随 3.1/3.4 落入 `07_Web配置.md`。

## 一、机制规则（三层分流）

进入新领域 Phase 前，先做概念缺口盘点，按三层分流，不是所有缺口都值得单独占段：

| 层 | 判定标准 | 处理方式 | 本框架实例 |
| --- | --- | --- | --- |
| ① 迁移复用 | Phase 1 已建立、换个模块出现 | 标注迁移点，直接用，不重学 | 见「四、迁移清单」 |
| ② 前置概念块 | 抽象机制类；被 ≥2 张任务卡依赖；不懂它无法追踪控制流 | knowledge-tutor 45min 独立块（本框架 C 块） | C1–C4 |
| ③ 卡内前置声明 | 领域惯用机制/命名/简单 API；单卡内讲清即可 | 铁律 21/22 卡内处理，不占段 | 见「三、卡内清单」 |

时机（2026-09-02 已定，不再「前置批量」）：

- C1：Phase 2 最先（✅ 2026-09-01）
- C2：插在 **2.2 之后、2.3B 之前**（25–30min 短块，带阅读清单）
- C4：并入 **3.1 开场** 15–20min（手写 GET/200 立刻对 handle_client_inner）
- C3：并入 **3.4 开场** 15–20min（两种 fd + shutdown 唤醒 accept）

## 二、四块课程地图

### Phase 2 机制块（C1 已过；C2 见上时机）

| 块 | 知识点 | 钩子问题（破坏式） | 纸笔操作 | 项目落点 | 八股卡方向 |
| --- | --- | --- | --- | --- | --- |
| C1 | 回调函数与发布-订阅模式 | cloud_ai 直接 `#include alarm.h` 去点灯行不行？今天加短信报警、明天加声光，要改几个文件？为什么走 event_bus 一个都不用改？回调写在 alarm.c，执行时跑在**谁的线程**里？ | 画"发布者→总线→订阅者数组"三泳道，标回调执行线程与此时锁的状态；改错题：回调里 sleep(5) 拖住谁 | `event_bus.c:21-177`（订阅/发布：锁内拷贝订阅者数组→解锁→逐个同步回调）；`alarm.c:19-162`（on_event 注册与回调体） | 回调为什么必须快返回、跑在哪个线程上下文 |
| C2 | 互斥锁+条件变量：等待/通知 | **先读** alarm.c 状态/on_event/等待循环/stop，再问：没事件时停在哪一行？on_event 改了哪个变量才 signal？ | 画 wait/signal 时序；一版「if + 先 signal 后 wait」找丢失唤醒 | `alarm.c:19-30,76-111,140-152`（GPIO 折叠到 2.3B） | 为什么 while 不是 if；持锁进 wait 的原子性 |

### Phase 3 机制块（不独立开块，并入 3.1 / 3.4 开场）

| 块 | 知识点 | 钩子问题（破坏式） | 纸笔操作 | 项目落点 | 八股卡方向 |
| --- | --- | --- | --- | --- | --- |
| C3 | socket 与 TCP 服务器生命周期 | 浏览器敲回车到 connect 成功，你的程序一行没跑，内核做了什么？listen fd 和连接 fd 为什么是**两个东西**？谁创建谁关闭？ | 画"监听fd→accept→连接fd→客户端线程→close"生命周期图，标两种 fd 各自的生灭点 | `web.c:932-1005`（web_start/web_thread/accept 循环）；`rtsp_server.c` 同为自研 socket server（对照） | listen fd 与连接 fd 的区别；每连接一线程模型 |
| C4 | HTTP 请求/响应模型 | curl 发出去的字节和你 telnet 手敲的，是不是同一种文本？web.c 为什么用 sprintf 就能拼出浏览器认识的响应？没有 Content-Length，接收方怎么知道正文在哪结束？ | 手写最小 GET 请求与 200 响应各一条（请求行/头/空行/体定界）；留一个 206 响应头空位给 3.3 | `web.c:840-930`（handle_client 请求解析与各 serve_* 写响应头）；`cloud_ai.c:72-197`（curl=同一协议的客户端侧，与 Phase 2 的 2.2 呼应） | HTTP 请求/响应基本结构；Content-Length 的定界作用；无状态 |

### 每块固定结构（与 Phase 1.5 框架五件套一致）

一句话定义 / 设计动机（不这样会怎样）/ 结构与样例（纸笔对象）/ 项目落点 / 面试变体；通过 = 学生独立重建前四件，落八股卡走状态行复测（复测按 08-21 规则暂缓至收尾）。

### 类比与已学桥梁（讲课时优先用）

- C1：报纸发行站（报社不认识每个读者，发行站=总线）；嵌入式桥=**中断上半部必须快返回**——回调跑在别人的关键路径上，与 ISR 里不能睡眠是同一条因果。
- C2：取号叫号（睡在椅子上等叫号，不是每秒问"到我了没"；被叫后抬头再确认屏幕=while 谓词）；V4L2 桥=**select/poll 阻塞等 fd 可读** vs 忙轮询 DQBUF，同是"等待/通知"。
- C3：客服总机/分机（总机永不占线=listen fd，接通转分机=accept 返回连接 fd）；V4L2 桥=**fd 都是内核对象句柄**，差异是 V4L2 一个 fd 反复 DQBUF、socket 每次 accept 生新 fd。
- C4：寄信（信封格式=头、信纸=体、空行=信封口，邮局只认格式不认内容）；无 V4L2 对应，属全新领域。

## 三、卡内轻量前置清单（铁律 21/22 处理，不占段）

| 概念 | 出现卡 | 卡内处理方式 |
| --- | --- | --- |
| base64 编码 | 2.2 | 一句话机制 + 4/3 膨胀比 + 为何图片要编码 |
| JSON 树（对象/数组/字符串嵌套） | 2.2 | mini 三件套（本项目响应 content 内还嵌一层 JSON 子串，双层解析） |
| curl easy API | 2.2 | L1：三步流程（句柄→setopt→perform），不背选项 |
| SQLite 表/行/句柄模型 | 2.3B | mini 三件套（类比快递分拣入库/按单取件） |
| GPIO 概念 | 2.4 | 1 分钟：芯片引脚电平输出；-1=禁用该路 |
| multipart/x-mixed-replace boundary | 3.2 | 卡内讲（类比：连续寄包裹，每件贴分隔条） |
| HTTP Range/206/416 | 3.3 | 卡内讲 + 样例演算（C4 已留 206 接口） |
| .tmp+rename 原子写 | 3.5 | 卡内讲（已有 1.7A g_cfg=副本基础） |

## 四、从 Phase 1 直接迁移的概念（标注迁移点，不重学）

| 已建立概念 | 来源 | 迁移到 |
| --- | --- | --- |
| 三线程边界、锁粒度小、"深拷贝断别名" | 1.7C | 2.1 三线程泳道图、2.3 回调与锁 |
| 最新值单槽 vs 队列、吞吐节流 | 1.7 | 2.1 判断 `ai_max_queue` 未被消费、"最新帧采样 vs 排队处理" |
| g_cfg=模块私有副本、热更新改副本 | 1.7A | 2.4 热启停、3.5 生效边界 |
| main.c 停机顺序、join 等线程退出 | 1.7A 跨模块生命周期 | 2.4 stop/join、3.4 web_stop |
| send 全量发送 vs 短写风险 | 1.5B/1.7 旁路 | 3.2/3.3 短写审计（对照 rtsp_server 的 send_all） |
