# EdgeFusion 学习进度

> 更新日期：2026-08-22  
> 本文件只记录实时进度、掌握度和接续点。完整任务、时间与验收标准以 `EdgeFusion学习计划与验收标准.md` 为唯一事实源。

## 当前位置

- 当前 Phase：**Phase 3 已于 2026-09-20 全收口（阶段总验收通过）——主线模块精读（Phase 0/1/2/3）全部完成**；下一站待用户排期：简历完善专场 / Phase 5 / Phase 6（Phase 1 已于 2026-08-28、Phase 2 已于 2026-09-16 全收口）
- 当前任务：**收口同步已全部执行（09-20）：Phase 3 总验收通过+五文档同步+八股 07 落卡 20 张+git commit。下一专场由用户定：口令「简历完善」（v5 迭代）或「继续 Phase 5」（V821 概念）**（3.5 已于 2026-09-20 收口：三场景+闭卷全过，详 09-20 学习记录；3.4 已于 09-19、3.3 已于 09-18、3.1/3.2 均 09-17 收口）
- 当前子任务：**3.1✅（09-17）→ 3.2✅（09-17）→ 3.3✅（09-18）→ 3.4✅（09-19）→ 3.5✅（09-20）→ 3.6✅（09-20，AI 代做+阶段总验收）= Phase 3 闭合**；速讲轨道已于 09-14 终结（8/9 段过闸、段 9 取消，产出文件留存）
- 全路线导航：[`全路线教学任务索引.md`](全路线教学任务索引.md)
- Phase 1执行手册：[`Phase1任务验收.md`](Phase1任务验收.md)（1.5A–1.9共11张任务卡）
- 当前动作：**Phase 3 总验收已于 09-20 全场通过（C1 四问+三场景+闭卷 8 题+变式 V+90s 复述，三必对全过；两处实然推翻题设=场景二 conf_save 盘满 500 不可达(282-284 返回值未查→缺陷#26 学生独立发现)/场景三 htons 截断 70000→4464 起在无人知端口+日志 1085 谎报；3.6 动手=AI 全代做，validate_conf 102 行已插入 web.c:730-831+调用点 909，矩阵+4 问参考答案+4 条残余边界已交付，状态=静态完成未板端验证）——收口同步已于同日执行完毕（五文档+八股 07 共 20 卡+git commit）；**用户 09-20 宣布下一专场=简历完善（现有 v5 基础上迭代，评估哪些技术点/战绩值得替换；新弹药=Phase3 全场收口句+两发实然推翻（500 不可达/htons 截断）+validate_conf 动手故事+缺陷#26+90s 阶段复述；投递闸门 Phase2/3 完成✅、板端跑通❌）**；3.5 已于 2026-09-20 收口（详 09-20 学习记录；**3.1–3.6 用时均未报告，待报告后按铁律 29 回弹 Phase 3 预算**）。Phase 1.5 实现层回补池待排（Phase 3 窗口已结束）；agent 三文件改动（AGENTS/nvr-tutor/nvr-knowledge-tutor=铁律 29 出处）未 commit；台账#4「socket 留 3.1」与铁律 29「留 3.4」冲突，加不加备注待用户拍板。

- 2026-09-20（主线·3.6+**Phase 3 总验收收口，Phase 3 全闭合**）：**3.6 动手=AI 全代做（用户要求，对齐 1.8 先例，掌握证据以验收场为准）+段 3 应用户要求升格为阶段级总验收（对齐 1.9/2.5 规格：C1+三场景+闭卷8题+变式V+90s复述，三必对全过；用时未报告，3.1–3.5 用时同样未报告，Phase 3 预算回弹待报告）**。①动手产出：`validate_conf` 102 行纯插入落码 web.c:730-831+调用点 909（free(js) 后、conf_save 前；失败 400+字段级 error；纯插入 0 行删除 git diff 核实）；交付完整校验矩阵（13 字段合法域+消费端拒绝理由：web_mjpeg_fps≤0→frame_grabber.c:138 守卫关节流白烧满功率/ai_fps≤0→cloud_ai.c:302 三目静默退 1s/segment_seconds≤0→recorder.c:226 无守卫每关键帧切段/掩码 strstr 通吃两形态）+验收 4 问参考答案+4 条残余边界（超长静默截断/满宽无 NUL 窗口/json_get_int=atoi 吞类型/掩码误拒取舍）；状态=**静态完成✅板端已验证❌**（本机不编译）。②C1 四问零纠偏过（pthread_detach 两步出生 attr=NULL joinable→1099 立即转 detached=3.4 档案外自读新细节；close(cfd) 唯一点 1061+pthread_create 失败分支 closer 互斥；**102 行插入后行号坐标现场重对、全场零漂移**）。③三场景全过：场景一（Wi-Fi 瞬断=三分支无分区支;两阶段细化「缓冲满前白跑/满后冻结只剩内核重传~15.4min」;free 四条路径+199 防御行实然不可达;N 死客户端=N 栈+N 副本+N SO_SNDBUF 有界;旗够不到/刀只拆监听 fd 门牌/exit_group 唯一刽子手）;场景二（**实然推翻题设第一发：conf.c:282-284 fflush/fsync/fclose 返回值全未查→真实盘满 500 不可达（唯一路径=fopen tmp 创建即失败 251）→残缺 tmp 被 rename 转正+POST 200+下次启动 fgets 半行命中=3.5 半行加载回响，全事故链成立=缺陷 #26 学生独立断言 tutor 核实入 memory**;conf_save「读旧写新保注释」结构（246 fopen "r" 逐行替换）=自读补档）;场景三（**实然推翻第二发：htons 吃 uint16_t 把 70000 截成 4464→bind 成功 Web 活在无人知端口+web.c:1085 日志谎报 70000=事实性错误日志**;「等 bind 报错=等一个永远不会响的铃」;main.c:161-163 LOG_WRN 继续跑=Web 旁路不拖主链;精修=实然层修复通道是「地址失联」非「死亡」）。④闭卷 8 题过（三必对全对;**题 1 一处纠偏=M-011**：两个 setInterval 实为时钟 1000ms+pollStatus→/api/status 4000ms（app.js:125-126），loadEvents(409) 调用者全是按钮（刷新/筛选/翻页）→状态=定时轮询拉/事件=用户驱动按需拉、事件列表无实时感;新句=「转正是 commit 宣称不能早于事实」「shutdown 负责打断 g_running 负责裁决」「流结束不在流内容里在 TCP FIN 里（--framebound-- 从不发）」;学生自评「1/2/4/6 直讲+迁移混合不计独立首证」诚实准确）。⑤变式 V（fps=0 双字段）超规格过：**新鲜度量化**（产 25fps 每抽最多 40ms 旧 vs 产 5fps 最多 200ms 旧=5 倍 CPU 换延迟略降）/「**配置值本身不携带语义，0 的含义由消费端守卫的形状偶然决定**」「防御缺位时的行为不是设计出来的，是碰巧长成那样的」/「旧固件 0 是两个消费端各自的方言，新固件 0 根本进不了门——入口统一词汇表」;学生纠 tutor 题面锚点错误（ai_fps 消费=conf.c:300 cloud_ai_reload 非 302）成立入档。⑥90s 复述过（四条腿+最值钱 tmp+rename「目录项语义让掉电难题变免费赠品」+最隐蔽 web_start）。⑦总验收后学生元问题催生新框架卡：「**每跨一次异步边界，成功语义就降级一次**」（发起成功≠完成成功;家族四实例 web_start/send/rename/fflush;修法三向=同步化/完成侧回传/完成侧自证——与「锁是延迟的唯一传染媒介」「断链在消费端」同格律）。⑧八股 07 建立=20 卡（3.1×3/3.2×3/3.3×3/3.4×4/3.5×3/3.6+总验收×4;3.1–3.5 候选按铁律 29 押后至此一次落卡）。⑨Phase 3 收口五文档同步执行（手册/progress/档案/索引/总计划）+memory #26 落档。**下一步：Phase 5（V821 概念）与 Phase 6 待排;用户宣布下一专场=简历完善（v5 基础上迭代技术点/战绩，口令「简历完善」）;挂起=validate_conf 板端验证、到期复测继续暂缓累积（含八股 07 新 20 张）。**
- 2026-09-20（主线·3.5 收口）：**3.5 · conf 原子保存与热更新边界通过（三场景+闭卷全过；场景三经用户深度校准压缩执行——掩码题砍第二轮漂移推演，保留往返一致性框架句+一轮推演+修法选边；用时未报告）**。①场景一原子写三题强通过：O_TRUNC 瞬间截断+「极端运气下旧文件还在=截断与新数据都未落盘」自挖深度+fgets 半行值被当合法配置加载（rtsp_url 拦腰斩断照样命中）+静默回退对照 2.3A 三级可观测性（默认值 -1/0.6f grep 核实全中）；四步分层精确（fflush 防进程崩溃/fsync 唯一对抗掉电/fclose 只关门/rename 改目录项）+原创收口句「**数据必须先于名字落盘**」+「fsync 破→rename 后掉电=损坏新文件且旧 inode 无退路」；Q3 超规格=rename 返 0 是内核可见性≠持久性+ext4 日志回滚方向分析（「回滚方向是旧，原子性没破，破的是持久性」）+dirfd 修法（open 父目录 O_RDONLY→fsync(dirfd)）。备案两句：ext4 ordered 元数据提交前先刷数据（现实多一层保护，通用答案不依赖）；fsync 须 fd 存活故排 fclose 前（顺序的操作性约束）。②场景二三栏表通过：立表标准「看 c:823 推送线上有没有人接」+web_port fd 根因四步（g_port=cfg 一次性拷 c:1007[web_start 内，学生误称 web_init]/bind 那刻端口进内核 socket 对象无人再查/「就算改 g_port 已 bind 的 fd 不挪窝」/新端口唯一出生时机=下次启动重走 socket→bind）=3.4 弹药兑现；Q5 强过=破表象「两个字段都被推送，差异不在推没推在消费端怎么读」（frame_grabber.c:138-139 每帧现读 vs cloud_ai.c:302 出生快照，c:356 喂了自存副本）+收口句「**字段被推送≠字段被消费，断链永远断在消费端**」（「生效时点=消费时点」深化版）。精修三处：拷了不重读栏混两子类（ai_fps=喂了不读线程重启可恢复 vs rtsp_url/ai_frame_width/mqtt_*=根本没喂须进程重启）；**GPIO 编号归错栏**（alarm.c:158-159 锁内确更新 g_led/g_buzzer 下一事件现读=软件层真热更，但 export/direction 是 init 对旧编号一次性→新编号 sysfs 路径不存在 write 失败=热更反而致哑；给判定法补第三条腿「消费时依赖的资源是否就绪」=2.3B direction 失败链换情境）；storage_dir 漏列须重启栏。③场景三（压缩版）三题全过：Q6' 行号 614/775/776/816/218 全中+「rename 转正」+旧 inode 注销=场景一迁移+401 下游（2.4 七路诊断①回响）+**全场最佳=不动点观察**（掩码函数 f(x)=前8+**** 对污染值还原自身→事故自锁稳定且界面不可见）；Q7'「防线按值长什么样拦不按哪个字段拦」「不是待遇不同是长得不像暗号的程度不同」；Q8 三侧代价精确（B=按值猜意图静默吞输入/C=斩断合法换 token 路径重启即丢）+选 A 三理由（不用猜/向 mqtt_pass 安全同构看齐/纯功能损失最便宜）+顺手修 conf.c:196 过时注释=M-010 出处历史回照；备案=第四条路前端不回传敏感字段（REST 惯例，超最小改动框架）。④闭卷两题强通过：C1 五步链（打底 745/覆盖/落盘 816/推送 823/转正 826）+保存失败分支=500 后 return 第 4、5 步整段夭折+**新值所有权收尾**（活在栈帧/死在 return/没进文件没进 g_cfg 没进运行模块）；C2 陷阱识别（保存成功后掉电两种顺序重启终态一致——「顺序影响失败分支和中间窗口，不是成功后的重启结果」）+窗口方向（现顺序掉电=改动迟到向前一致/对调后=向后回滚）+收口句「**凡已推送必已入库，运行态永远不领先于持久态**」+WAL 家族挂接（先落盘后生效；tutor 半句丰富=严格 WAL 是意图日志+数据页两份，此处新值文件即持久意图=同族单文件版，3.1 教学标签「先存后推」）。⑤八股 07 候选登记（3.6 落卡）：数据先于名字落盘/四步分层/rename 返 0=可见性≠持久性+目录 fsync 缺口/断链在消费端+判定法三腿（现读 vs 快照 vs 资源就绪）/掩码不动点+按值拦不按字段拦/凡已推送必已入库（先存后推 WAL 族）/GPIO 名义热更实际致哑（资源就绪腿）。下一步=3.6（校验动手+Phase 3 收口，口令「继续 3.6」）。
- 2026-09-19（主线·3.4 收口）：**3.4 · socket 与客户端线程生命周期通过（吃进 C3 核对；三段+闭卷全过，多处超规格；用时未报告）**。①段1：C3 核对超规格——总机/分机模型+自创「三条铁律」（一号多机/总机永不聊天/分机不串线）；K2「数据到达时归属有没有歧义」判据+「listen fd 只有半个五元组键没法分拣」+「TCP 连接=独立状态机，状态挂在会话上句柄就得开到会话上」=Q2 免答判过；精修=行号 2 处（socket 972/listen 979）+五元组计数；机制精修=**backlog 全连接队列里是已完成握手等 accept 的连接**→connect 返 0≠accept 已返（时序差家族第一兄弟）；Q1 生死簿全过（「两条命两个凶手两个时机」+置-1 防二次关+断流题时序可算=200ms 节拍+当帧发送时间）；归因钉死「拍醒 accept 的是 shutdown 非 g_running」（学生两次摆动后归位）；c:995 漏答两次并入段2 快问。②快问+Q3 全过超规格：**收尾责任跟所有权实际归属走**（交接成功归服务员 c:959/失败弹回门卫 c:995，与 get_jpeg malloc 家族同构）+EAGAIN 成因；3a 醒后路径全对+「同一个 c:991 平时是重试、停机时是送客（错误处理与停机解耦）」；3b unlink-fd 同族自主迁移（close 摘 fd 表项/阻塞中 accept 持内核引用死不透/「拆门牌吵不醒屋里人」）+**幽灵总机双分支自推**（无新客=双双永眠/有新客=唤醒干完那单见旗倒正常退场；新 cfd 不依赖被 close 的监听 fd 号）；3c join 挪前=死锁环/先刀后旗=不死但忙旋（病根=c:991 无条件 continue 隐含「错误偶发、重试免费」假设）+「三步链的正确性不在每步本身，在箭头方向」；精修=线程栈 8MB lazy 分配+errno 不赌死（EINVAL/ENOTCONN 记结论即可）。③Q4 经用户元问题瘦身（「是否外部细节」→裁定 4a C 语言 static 机制超纲不考/4d grep 结果直给=4 模块 4 把私有旗连类型都不统一；主体保留=停机传导架构+复发点）：4b 九站传导链全对+行号自核全中（SIGINT→handler 只清 main 家旗→EINTR 提前醒→谓词退环→函数调用串逐个点名→各家旗+刀+join；「信号只负责打断，真正的停机是同步调用一路串到底」）——**g_running「整机停机标志」复发点（3.2 登记+3.4 Q1「广播效应」第三次滑）正式关闭**；4c 超规格=「旗治走着的，刀治睡着的」+「旗是贴在墙上的通知，只对路过的人有效」+三反事实+自主精化**「分界不在线程身份在睡姿——轮询者碰上不收的对端即退化睡客」**（3.2 慢客户端 send 卡死与今天 accept 卡死统一框架，修法 shutdown cfd/SO_SNDTIMEO 与 3.2「卡死药」一致）=跨卡整合全场最佳。④Q5 全过：5a「唯一失败路径只覆盖发车环节的同步错误」+「返回 0 语义=启动指令发起成功」+**「两条时间线从不相交」**；5b 谎旗（2.4 running 说谎家族自主迁移）+「有界泄漏」+shutdown 对未绑 socket 回 ENOTCONN 无害+join 秒回「停机全程顺利掩盖停的是早死的服务」；5c「防重复启动的哨兵查的恰好是失败路径忘了降的那面旗」+新配置连 c:1006-1009 的门都进不了+「所有 static 复位寄生在进程死亡上」+五环因果链；5d 方向一（同步化：该异步的是长期服务不是一次性挂牌）独立完成，方向二（异步握手）AI 辅助自报不计独立证据→C3 复述转化（while 防虚假唤醒=C2 存货自主嫁接）后计掌握。⑤闭卷三题全过：C1 三泳道总装（三时序差打点：start 返 0≠挂牌/cfd 交接=异步所有权移交/join 只收门卫；「handle_client_inner 只是借用，所有权人统一关、多个提前 return 不漏」；fd 死法对表+分岔点=所有权移交；收口「发射后不管/所有权易主/先旗后刀再收尸」）；C2 三线程命运变式超规格（A 轮询者≤一拍退+send 阻塞退化睡客例外/B「c:437 谓词 sent<content_len 全程无旗，不是抗命是压根听不见命令」/C 单程毫秒级（grep 证：全 web.c 读旗仅 c:195/c:985）/exit_group 连坐「进程退出是 detached 线程唯一的刽子手」/第三兄弟=**web_stop 返回≠全员退场**，「调用返回的那一刻恰恰是时序差最大的那一刻」）；C3 复述过。⑥八股 07 候选登记（3.6 落卡）：两条时间线从不相交/c:991 平时重试停机送客/close 拆门牌 shutdown 打对象/三步链在箭头方向/旗治走着刀治睡着+睡姿分界（跨卡统一）/收尾责任跟所有权走/web_stop 返回≠全员退场/发车 vs 挂牌环节/曾经启动过 vs 活着/cond_wait 把 return 变成一道闸/幽灵总机双分支。下一步=3.5（conf 原子保存+热更新，口令「继续 3.5」；3.4 已供弹药：web_port/web_bind 不能热更的根因=监听 fd 一次性 bind、无重绑机制）。
- 2026-09-18（主线·3.3 收口）：**3.3 · MP4 HTTP Range 通过（三段+闭卷+V2/V3 全过；开块背景交代前置精修入教学法记忆——四层目的须成段展开，禁压缩）**。①段1 演算全过：三样例（无头 200+Accept-Ranges 能力广告/`bytes=1000-1999` 闭区间 1000 非 999/`bytes=4000-` 右端省略=c:396 默认值+c:403 守卫拦截天然实现非钳制）+三头分工（Content-Length 206=e-s+1 与实发对齐、total 由 /total 尾巴携带不重复）；精修1=range_hdr 实为空串非 NULL（c:913 栈缓冲初始化+req_hdr_get 找不到头不写；c:399 两半守卫各防各，NULL 半在本项目永不触发=防御性写法）；精修2=Q2 Content-Range 整行被截断补全+措辞「右端省略的闭区间」。②段2 强过：场景A 越界重置推演（start=9999→c:406 重置 0/end=4999/partial=1→206 `bytes 0-4999/5000` 全文「顶着 206 的皮」）+标准对照（合规菜单只有 416(带 bytes */5000,SHOULD)或 200 两道菜,「206 换内容是第三种」）+客户端三层代价（整票带宽/错误不收敛无纠错信号/好玩家付带宽坏玩家花屏——「两种玩家都付代价,货币不同」）；场景B 两层分界独立构造（Range 词汇表只有字节偏移无时间,时间→字节翻译在客户端靠容器索引,接力=查目录算 X→发 bytes=X-(恰为 Q3 形态)→lseek→206→从 sync sample 起喂解码器）+收口句「智能在客户端,dumb 在服务端」+方法「先问每层的词汇表」；stts/stsc/stco/stss/sidx 样本表名与 tfd 机制=辅助层申报,不计独立证据（铁律 29）,分界句本身独立推出=验收点。③段3 强过：六步生命周期全对（fstat 返回值未查/send 头返回值未查均自报）；lseek=只改内核 file description 偏移数字 O(1),500MB@90% 丢弃法 10 倍读放大+SD/eMMC 带宽与录像写争用+页缓存冲刷,「丢弃法浪费随 seek 位置线性增长」；want 双钳制保 CL 诺言不发一字节；三出口各对事件（read≤0=文件侧先断/send<0=对端先走/谓词假=唯一正常出口）；半截响应=curl "transfer closed with N bytes remaining"+CL 白纸黑娘才可检；**unlink/fd/inode 保活未标待验证直接答对**（unlink 只摘目录项+文件变大无害=want 框死）=3.4 fd 生死簿预存粮；精修三条=`bytes=abc` 垃圾数字→strtol 0→206 0-total-1（与越界重置同族「206 换内容」）/Connection:close 是宣言,机械 close 在外层 handle_client_inner c:959/read=0 三层证据（通用 O_TRUNC 成因对;本项目实然=段文件名时间戳不复用+cleanup unlink 非 truncate→近乎不可达,c:441 主防 read=-1 介质故障——解释作者为何敢省写）。④闭卷强过（未见过的 start>end 倒置区间 `bytes=3000-1000`）：content_len=-1999 无人拦/while 谓词 0<-1999 假「压根不进门」非提前 break/lseek(3000) 白跳零字节 body/三重畸形（206+倒置 Content-Range+负 CL,Chrome ERR_INVALID_RESPONSE）/缺 start≤end 关系守卫「守卫矩阵三格只铺了两格」+定性=非法输入被静默改写而非拒绝,与场景A 同宗。⑤变式过：V2 三个「有没有」（总长/字节↔内容映射稳不稳/能否回头）+multipart=「连接本身即时间轴」；V3 单 moov 集中目录两笔账（表随片长线性膨胀 2h@30fps≈21万样本数 MB+绝对偏移 stco 须数据落盘后回填致 moov 钉尾,播任何一秒先 Range 拉尾部几 MB）vs fMP4 章首小目录（moof 带 tfdt 跳读/二分探底/每探针几百字节 Range）——**顺带补完 1.6B movflags 选型 why（frag_keyframe+empty_moov=弃集中目录每章自带小目录）,并接 1.9 faststart 遗留变式（faststart=后处理搬 moov 到头,本项目 fMP4 从根免掉）**。⑥**3.3 用时未报告**；八股 07 候选登记（3.6 落卡）：越界重置与「206 换内容」审计句/两层分界句（智能在客户端 dumb 在服务端）/三头分工/lseek O(1) vs 读丢弃/守卫矩阵三格/三个有没有。下一步=3.4（socket 生命周期,吃进 C3,口令「继续 3.4」）。
- 2026-09-17（主线·3.2 收口）：**3.2 · MJPEG 预览通过（三场景+闭卷，按铁律 29 重写版执行；多处超规格）**。①深度校准（用户主动问「这种深度面试会考吗」）：主场层=SIGPIPE 语义/阻塞 send 无超时/短写/线程隔离/multipart 定界/fps 三栏；项目自研句层=尾包 c:211 不查+empty_count 有界等待；提升池=RST 时序竞态拆分/errno 细分/TCP 重传量级——后续验收按此口径执行。②场景一全过：Q2 超规格三分支（RST 已到→c:210 -1 干净退场/RST 在途→c:211 失败被吞、退出推迟一轮=200ms+白做一轮 get_jpeg/无 RST 分区→send 卡死 c:210，已核 web 全文无 SO_SNDTIMEO/非阻塞，仅 listen fd 有 SO_REUSEADDR c:974）；Q1 强（无帧分支 continue 跳过清零=只有真帧能救活+三处精修：usleep 可被信号打断 EINTR 本项目等效全睡/g_running 实为 web 私有旗 c:32 唯一清 0 者 web_stop c:1024/get_jpeg 严格三出口、c:199 防御行与 c:196 预初始化配对）；Q3 卡壳后直讲、一句复述通过=**SIGPIPE 复发登记（2.5 五场景前一日以超题面答过同链，换 web 情境检索失败，#52/#59 口述复发族+1，收尾复测池）**；Q4 强（协议承诺不可撤回=503 结构性不可能/常驻资源封顶 vs ai_thread 等待成本≈0/重连窗口 10s+空轮 100ms 快检；**纠偏 1 处=前端自愈环不成立**：imgFail app.js:185-191 只亮 NO SIGNAL 不重连，c:180 Date.now() 重设 src 在 toggleStream 手动恢复分支——AI 辅助层细节记错实例，印证 2.5 证据分层口径）。③场景二强通过：Q1 零修正（消费者重读正例=frame_grabber.c:138-139 每帧现读 vs cloud_ai.c:302 出生缓存半生效+自报 data race 注脚=1.7C 审计表 target_fps 读写不对称原案复用）；Q2 **超规格自建「缓存栏」三栏模型**（产帧 target_fps→g_jpeg latest-wins→发送 200ms 写死；金句「MJPEG 无新帧概念只有最新概念」「发送侧是照节拍抽缓存的泵，不是照事件推流的闸」；产 2fps 同帧重复上网线 2.5 遍/调 10 白烧；精修=白烧清单无解码——1.7B 存货：节流在解码后挡缩放+编码，参考链跳不掉）；Q3 口径三词收口（热更的是产帧/钳死的是发送/名字暗示的可见帧率两头没承诺）。④场景三全过：Q1 三层清单（栈=detached client_thread 钉死 send 内、web_stop 只 join 监听线程不收割 c:1023-1024/堆=jpeg 副本 free 推迟 c:212/内核=SO_SNDBUF 旧帧陪占）；Q2 双支柱总结（线程模型+拷贝交割错开共享窗口与慢窗口=1.9「锁划得小/深拷贝断别名」双保证落地）+「锁是延迟的唯一传染媒介」复用；Q3 短写三拍全对（n==sz 也只到内核缓冲不上网线/流内错位链=CL 声明 sz 实到 n→下 part 头被吞→破图/阻塞模式短写罕见=EINTR 场景）；Q4 修法=send_all 迁移方案逐行核源码无误（rtsp_server.c:190-199：MSG_NOSIGNAL c:194+EINTR continue c:195；三处替换表+c:211 唯一同时修短写+盲区+jpeg cast）——**收面试弹药+登记动手池候选（本次不动原始文件）**；补分界线=send_all 治短写不治卡死，卡死药是 SO_SNDTIMEO/非阻塞+epoll（3.4 正面攻）。⑤闭卷通过（强）：循环轮骨架三分支全对+**自我纠错=每轮三笔 send 非「四次」（流头在循环外一次性，tutor 评分点措辞含糊被学生纠正）**；nano 复发 1 处=g_running 归属再说「整机停机标志」（精修复发，收尾复测池）。⑥沉淀入 3.6 八股池：缓存栏三栏/双支柱/SIGPIPE 换情境句/两个病两个药（短写 vs 卡死）/自愈环修正版（断流亮 NO SIGNAL 手动恢复）。**3.2 用时未报告**。下一步=3.3（Range，口令「继续 3.3」）。
- 2026-09-17（主线·3.1 收口）：**3.1 · Web 路由与 API 地图通过（C4 核对+B 组四问+闭卷，按当日新铁律 29 重构版执行；同日用户改 agent 三文件=铁律 29 出处，未 commit）**。①C4 核对强通过：A1 origin-form 请求行+完整 URL 只活 Host 头（应然 RFC 强制/实然从不读分层）+52 字节自数+sscanf 宽度与数组配套；A2 四段=语法结构非四次 send（实现只两次 123/124）+「头是预告，关闭主体是 c:959」+裸 404 无 CL→read-until-close，FIN 保证链=穷举 close 出口含建线程失败分支 c:995；精修 2 处=404 裸串 45 字节非 43/RST 收窄为「宣言与行为不一致→客户端行为未定义」。②B 组四问：B1 三身份强通过（血缘三形态=内存最新值/重复拉成视频/落盘定档；503 暂时/200 流内静默/404 永久三故障哲学；重试主体=「三种消费者三种容忍度」2.5 复用）；B2 强通过（**M-009 钉死句式一字不差=三次复发后第二次独立复述正确**；「web.c 全文无通知路径」缺失证据法；网络推两处=RTSP stream_receiver.c:168+MJPEG；两腿=起步拉/数据推）；B3 强通过（先存后推防「内存热更/盘上没记」分裂+失败 500 短路/补丁语义 JSON 出现才覆盖/745↔826 基底回流/web_bind_port 重启才生效 c:825）；B4 通过（三桶 4+6+1+悬空端点 /api/event/<id> 前端零调用；精修 3 处=multipart 顶层 ctype 不属 image/*，判据收准「JSON=状态面/html=页面/其余字节流含 multipart=媒体」/ctype 是 send_http 第三形参/cloud_ai_reload 更新的是 cloud_ai 自存副本非 g_cfg，fps 线程重启才生效结论与 2.4 一致）。③闭卷：C1 六步强通过（超题面观察=**CL 解析+补读块对 GET 也跑**、分发前方法无关；Range 预取卡 GET 分支梯子前；free(post_body) NULL 安全；函数不关连接 close 在外层 959）；V1 通过（GET=按 URL 取资源、静态文件是合法资源族/POST=提交给处理过程；「读可宽容、写必须白名单（一条路一条命）」+「没有文件桶 app.js 到不了浏览器，API 一个请求都收不到」超题面因果）。④学生闭卷前口述练习自纠错误清单 7 条，按用户要求过滤：**保留 3 条**——E1（时间解耦≠异步；推拉=谁发起交接/同步异步=发起方等不等；精修：「全库唯一异步=cond_signal」说满，accept→detached client_thread 亦是异步交接）/E3（handler 不换线程，每连接一线程在 accept 层定=3.2 慢客户端占线程地基）/E7（路由键=method+path，query 875 剥掉；口诀「method 选支路、path 选 handler、query/body 是货」）；**剔除 4 条**——E2/E4/E5（口误/归类/措辞类）+E6（已由本会话 A2 精修覆盖，不重复建档）；GET 文件桶语义根源（HTTP 生于取文件，API 是嫁接）作概念素材入 3.6 八股池。⑤**3.1 用时未报告——铁律 29 要求实测后回弹 Phase 3 预算**。⑥八股 07 与五文档同步按铁律 29 留 3.6。下一步=3.2（get_jpeg 所有权 2.1 迁移+multipart 定界/两腿/200ms 上限/短写对照）。
- 2026-09-16（主线·2.5 收口+**Phase 2 全收口**）：**2.5 · Phase 2 闭卷验收一场全过（段1 三项+闭卷 8/8+变式 V-c）**。①段1：四泳道强通过（数据/所有权/同步异步三要素全+行号自核几乎全中：frame_grabber 159-166/event_bus 128-132/web 473 分毫不差；两修=MJPEG 分腿（对内 200ms 轮询拉/对浏览器一次 GET 后持续推）/row_to_event 归属 event_bus.c:183 非 web——query_filtered 与 get_jpeg=同族「模块拷进调用方容器」输出参数契约）；五场景强通过（SIGPIPE 忽略前提+不忽略则 EPIPE 杀全进程=超题面加分；GPIO -1 软禁用引 alarm.c:122 注释原文）；30 秒复述通过（内容零错；四处术语精修：「异步拷」→同步写异步消费/「Web 全程拉」→事件拉+网络推两处（MJPEG /stream web.c:928、RTP）+进程内回调推（非网络）/「按 ai_fps 周期醒」→配置意图 vs 实然 7s/「V821 推流」→连接方向（网关 client 拉）vs 字节方向两维度；530 字≈90-120s 超规格→留双版本：30s 骨架版+90s 详细版）。**②tutor 自我纠错一次（入档）**：五场景轮把 /api/snapshot（活帧端点，web.c:219 get_jpeg+224 无帧立即 503）与 /snap/evt_<id>.jpg（落盘文件，web.c:533 event_bus_get(id)→open(ev.jpeg_path)）混谈，对学生正确答案「三种消费者三种容忍度」（ai_thread 无限轮询等/mjpeg 有界等 101 次×100ms≈10s/抓图零等待 503）误发纠偏，源码核验后撤销、学生原答成立——三层证据法对 tutor 同样生效。③闭卷 8/8（三必对 1/3/6 全对）：Q1 三层反事实（realloc 搬家→悬垂/原地 memcpy→撕裂/全程持锁→慢 IO 拖 emit）+「让数据生命周期脱离锁」；Q2 7s≈0.14fps+上限节流≠周期节流+修法（记起点、sleep interval−elapsed）+宁可降采样不积压取舍；Q3 sleep5s 拖 ai_thread 栈+三层次生影响（采样率 7s→12s/拖未来发布者/锁已放不死锁不阻塞查询）+「一个订阅者的慢不该传染给总线的其它使用者」；Q4 none=调用点约定非总线不变式（c:276；publish 全文无类型校验）+新发布者推 none 全流程推演（conf.c:77 默认 trigger_types 精确命中）；Q5 细节五条（时长值交接非事件排队/while 谓词防虚假唤醒/判断与 signal 同锁无丢失/reload 改配置下一触发才生效（wait_s 已拷局部）/stop 双 g_running 检查点+join 后补 GPIO 置低；行号 27/28/81-82/104-108/145/147 全中）；Q6 拉+双依据（服务端一问一答+前端无 /api/events 定时器；独立结论靠 2.3B 推拉四证据）；Q7 控流证据法「快照段与回调段之间无 return」+jpeg_path 空串→"snapshot":""→前端「无快照」占位（app.js:431）+孤儿半文件+「先库后图=事件比图重要」取舍；Q8 三分支守卫=各自 init 标志（163-169）非 ai_enable+fg 参数名带 ai_ 前缀但服务双消费者辨析+「AI 只读产成品不管产线」+边界自觉（fg init 失败=预览+AI 一起死，共享非耦合）。**④诚实申报分层**：web/app.js 细节（路由行号/Connection: close/双 setInterval/app.js 行为）系学生 AI 辅助补全，不计独立证据；web 深读留 Phase 3 正式建立。⑤变式 V-c（TF 卡极慢事故；学生先提延后，经「知识足迹全在 Phase 2」拆解后选择当场作答）**强通过**：锁链=query_filtered（c:228）等 g_lock，publish 117-174 锁内串 INSERT/fwrite/UPDATE/cleanup 四个盘 I/O——「写者的盘速决定读者的响应」；录像无感=「**锁是延迟的唯一传染媒介**」（学生原创；recorder.c:89 avio+c:32 mtx 只保统计，tutor 核验全中）；响铃固定晚 ~2s 不丢（回调在 174 unlock 之后）；修法=缩临界区（锁内只留 INSERT+id+按确定性规则回填路径——evt_<id>.jpg 拿到 id 即已知；**交换物=long id+jpeg 缓冲所有权而非 ev 借用**；代价=读者容忍半文件=孤儿取舍再现）。⑥2 卡入八股 06（累计 33：快照 I/O 持锁事故链 V-c/三消费者三容忍度）。⑦Phase 2 收口五文档同步执行（Phase2手册/progress/学习掌握档案/索引/总计划）；错误台账新增 M-009（推拉方向三次复发后钉死）/M-010（token 落盘方向，2.4 纠偏移入台账）。**2.5 用时未报告（按预算坐标 85min≈1.4h；Phase 2 坐标≈9.4/9.5h）**。到期复测累计约 62 张继续暂缓（收尾统一）。下一步=3.1（口令「继续 3.1」，C4 并入开场）。
- 2026-09-16（主线·2.4 收口）：**2.4 · 热启停与失败降级 L2+闭卷全过（L1 速讲段2/段5 折算免做）**。①段1 四问全过：Q1 强通过（g_running 语义=「曾经启动过」/早退不清旗/running 说谎/线程自杀绕过清旗=预制审计③自主推出）；Q2 过（else 吞 00+11/四象限线程后果/「同一把旧尺子」+证据法=注释+缺失的代码行）；Q3 过（0→1 stop 两件活=join 收尸+curl cleanup 配对/旧 interval_us 随栈帧消亡）；Q4 强通过（字段三分类：每次现读 c:208/213/223/249=立即生效/出生快照 c:302=fps 拷了白拷/不拷=token 只活在形参里；自总结「生效时点=消费时点」判定法+自挖 11 分支 enable 不可能变+超题面行号自核 208/213/249 全对）。②封口快问过+纠偏一处：conf_save token 落盘方向记反（信了 conf.c:196 注释「敏感字段不落盘」，实然 c:218 白名单含 token 落盘真值）→三层证据法当场收口，最小动作修正=只需重启进程（POST 已落盘）；真掩码不对称方向=GET 回显脱敏 web.c:607-620 vs 落盘不脱敏。③段2 三问全过（均强通过）：Q5 join≠kill（枚举 setopt 清单 c:217-227 证明 XFERINFOFUNCTION 缺失=「以缺失为证据」家族/「出口的钥匙攥在 curl 手里」/CURLOPT_TIMEOUT 自纠为全程总闸）；Q6 停止延迟双例（「差别不在停法，在等待原语里检查点的密度」/alarm 不可中断段=代码自己造的边界 1s vs cloud=库造的边界 30s/旗+signal 缺一不可双向反证）；Q7 六路故障两管道+四层归类+诊断排序（①401=网络全通只错凭证→⑤500=云错→⑥=约定不齐修 prompt 不修网络→②③④=网络逐层深挖）+影响面（阻塞锁死 ai_thread 栈，旁路全活）+event_bus_init 附加问 tutor 补讲（订阅仍在/!g_db 一票否决）。统一核对四点：CONNECTTIMEOUT=段上限 vs TIMEOUT=总闸（连不上最迟 10s）/第三出口=出错提前返回/虚假唤醒脚注（只倒旗不 signal 的理想化缺口，挂死口径不变）/join 转发延迟次生链（1→0 POST 最长挂 30s vs 0→1 秒回不对称；安全≠快；在途帧归宿）。④闭卷 13/13 全绿零纠偏：V1 九行（甲 1→0：join 活线程+POST 响应≈30s+web 每连接一线程只有该客户端在等+「curl 卡 ai_thread 的栈，join 卡 web 线程的栈」镜像句；乙 0→1：守卫真·空转 vs 出厂僵尸旗场景对照+token 侧门 c:324+fps c:302 重算生效）；V2（else/timeout 消费者 c:223 每请求重读/停机 join≤5s/「setopt 烙印时刻」超题面边界）。⑤7 卡入八股 06（累计 31）。**2.4 用时未报告（按预算坐标 95min≈1.6h）**。三必答全过=join≠kill/fps 热更实然边界/慢云端不阻塞录像。同日教学法沉淀三条（用户反馈）：题干措辞精确化（变量名=取值+锚点，禁比喻进题干）/题目原子化试用即废→定稿「场景成段+段内串联」/均入 AGENTS.md 铁律 28。下一步=2.5 Phase 2 闭卷收口场。
- 2026-09-15（主线·2.3B 收口）：**2.3B · alarm 落地与 Web 查询支路 L2 全过**（L1 速讲段 2/4 折算=订阅者名单核对+L1 整理版复习交付；B1 骨架口述/B2 方向辨析/V1 GPIO=-1 三项已于 2.3A 会话段 B 预考通过不重验）。①段1 三问全过：Q1 单槽真覆盖窗口强通过——双窗口独立构造（未取走=真覆盖·最后写者赢·锁只防撕破不仲裁；响铃中=「存款不是销户」）+「被抹掉的是基数不是时长」+值语义（电平 vs 边沿）+g_jpeg 家族迁移（真相源另有其处，旁路只承诺别错过不承诺数清）；精修2=+=active_s 是一轮加长非两轮（计数才两轮+风暴自伤）/时长陪葬仅 reload 落两写之间。Q2 GPIO 失败链三条全对（export 三连静默每次响铃重演/direction 一次失败定终身不可自愈/失败停不下来>失败不响）+注释「只LOG_WRN」vs 零日志三层证据法独立摆出（运行层内核事实已补）。快查 reload min_conf 过=线程消费集无该变量/生效=下一事件到达。加分 strncpy 学生跳过、tutor 钉死（BSS 零初始化+reload 补 0 双保险，风险在隐式保证，入提升池）。②段2 五问全过：Q4 推拉四证据（路由表全 GET 无推送端点/publish 不碰 socket/app.js fetch/仅有的推=MJPEG+进程内回调）+事件停点三类接触者；**纠偏1处=「cleanup 字母序=id序」不成立**（evt_%ld 不补零 c:147+alphasort c:54→跨位数删新留旧、注释「删最旧」三层证据又一例；新发现入 TODO #21 删序错位/#22 无 evt_ 前缀过滤 I-01 族）。Q5 limit=200→64 两行不对齐+截断在 web 侧+修法=两数字同源常量。Q6 三人互堵+锁内四步分界（抄档案在门里装信封在门外）+两赌注受害面=审计①消费侧回响（题目表述过挤已认领，学生重建=原意）。Q7 能力设计非过滤设计（URL 是行号不是路径/打开路径来自 DB 引用）+穿越攻击两例验证（../conf/gateway.conf 拖 anthropic_auth_token=真实键名 conf.h:35）+精修=sscanf 不吃完整串闸松但不伤（反证能力论点）。Q8 event_t 有效期双支路=借用（同步嵌套+纪律边界诚实自划「ev 和 packet 一样是借的」）vs 拷贝（锁内冻结+线程私有=1.9 双保证独立部署）。③收口思考停机顺序三段式作答（join 栅栏=deinit 时天下无 publish/依赖箭头通则 vs 实际 UAF 牙齿分层/!g_db 守卫 c:112 一票否决+锁外 TOCTOU 针眼只在极限交错）——因另场直问已直讲在先，判**复述确认级**（恰好补上该记录要求的口头复述确认）；另场深究产出 TODO #23（守卫 TOCTOU）/#24（web 非最先停则 POST /api/config 可热复活已停模块）。④7 卡入八股 06（累计 24；含删序错位缺陷卡，审计①卡「unlink 最旧」措辞带修正）。**2.3B 用时未报告**。下一步=2.4（热启停与降级，停止延迟双例对比当主菜）。
- 2026-09-15（主线·2.3A 收口）：**2.3A · event_bus 发布链 L2 通过**（L1 速讲折算免做；C1 复收口一句过）。①失败矩阵四行全清：INSERT 败（return -1/ev->id=-1/无快照无回调/web 不可见+上游 cloud_ai.c:277-278 对 -1 无分支仅日志留痕）、fopen 败（零日志纯静默/行已在库路径空串）、短写（WRN 可观测+**孤儿半文件**学生自挖/else 加 remove 进动手池）、UPDATE 败（**三方分裂：内存有路径/盘上真有图/库里无记录**学生自创表述；step c:158 未查 vs prepare 已查）。②审计①过闸：受害面=web 四读路径（query_filtered c:228/count c:207/get c:265 锁内，调用点 web.c:474/256/516/547），MJPEG/RTSP/录像不碰 event_bus，无死锁无数据损坏；敢写两理由=单写者无写写竞争+耗时模型赌存储快。③Q6 因果环最强：真路径依赖 id→id 依赖 INSERT→环解不开，AUTOINCREMENT 键策略下先插后补是必然+换键可消 UPDATE+掉电窗口三病因同形。④段 B：B1 开卷口述替代默写（裁定合规，结构+因果全在场）；V1+方向辨析「凡通知必已入库」恒真（控制流单向）/「凡入库未必通知」三条路（订阅空/void 回调无送达保证/崩溃窗口）。⑤纠偏 3 处：cleanup_snapshots 实为目录扫描非按库引用（孤儿=有条件泄漏+可能删有主文件双向脱钩）/「推送侧报带快照」应然实然分层（alarm 不消费 jpeg_path）/读侧行号对调 207↔228。⑥4 卡入八股 06（累计 16）。**2.3A 用时未报告**。下一步=2.3B。同会话教学法沉淀：铁律 27（开块必交任务定位块+L1 整理版）+铁律 28（任务卡表格化、题目=完整问句、口述为主、模板=参照不强制），速讲轨道终结拆档。收尾 git commit 4b64309 已入库（含 2.3A 收口/速讲拆档/铁律 27/28/段 8 落档/纯注释改动/05 应答卡首次入库）；**push 第三次失败（GitHub 连接超时，同段 5 起网络原因），待网络恢复补做**。

- 2026-09-14（速讲→主线同步+段 9 挂起，未推进任务 ID）：①速讲轨道 8/9：段 7（五条人话版）、段 8（应答卡双线：05 讲义 11 卡+简历五条第一问 8 条实战）过闸；**段 9 彩排+拷打挂起，待主线完整模式完成后**（用户拍板，同挂=段 8 B 组 4 条+缺陷深挖修复回补池）；速讲旁路投入约 13h+，单独登记不计主线预算。②**速讲进度同步主线（折算审查后执行，五项折算审查全部成立）**：2.3A L1 免（速讲段 1=publish 五步+9/9 函数+框架+why，行号抽验全对，publish c:110 起 long 返回）、2.3B L1 压缩为核对订阅者名单、2.4 L1 免（alarm+cloud_ai 函数功能级；热启停/停止延迟/失败降级 L2+ 全保留）、Phase 3 的 web/conf L1 免+C3/C4 机制已声明（3.1 开场 C4 压到核对仍补套接字生命周期；3.x 闭卷不免）——Phase 3 手册不动，开卡时按速讲台账执行。口径=只免 L1 不免 L2/审计/闭卷；速讲口述不算掌握证据、不进掌握档案（2.3A 收口时 L1 来源随 L2 证据一并入档）。③Phase2 手册任务总表状态滞后（2.1/2.2/C2 未标完成）本次顺带修正。④待办：git push 补做（速讲段 5 起两次失败）。

- 2026-09-14（速讲轨道终结，未推进任务 ID）：用户宣布快速模式使命结束，拍板「拆骨架、文件全留」。已执行：删 `.claude/agents/nvr-quick-tour.md`；AGENTS.md/nvr-tutor.md 移除速讲口令路由与坐标系条目（「继续 2.3A」路由保留，改为无条件折算口径）；索引与本文件清除轨道挂起条目，**段 9 彩排取消**（彩排/拷打由 Phase 6 模拟面试承接；段 8 B 组身份边界 4 条+缺陷深挖修复仍在回补池）。`面试备战/项目速讲/` 8 文件全留：04 口述稿/05 应答卡=面试弹药，过码台账=主线 L1 折算事实源（「继续 2.3A」开场仍先读它）。速讲旁路累计约 13h+，不计主线预算。

- 2026-09-07（主线暂停·接续点存档；**09-14 已恢复主线，接续点以 09-14 条目为准**）：**用户决定秋招投递优先，主线打磨暂停（09-07~09-14）**。2.3A 实际已于 09-06/09-07 开块：任务卡两段表+L1 三问已发待答；SQLite 前置已直讲两轮（一轮=stmt 三件套/五步、「库文件/无独立线程」自答对；二轮=stmt 概念视图/不透明类型、bind 五存储类/动态类型、step 隐式事务=唯一碰 db 文件且耗时集中在 step+finalize/reset；已预告审计①扩展=step 磁盘写事务也在锁内）；学生待读三段后答 L1。**投递后复进口令=「继续 2.3A」（09-14 更新：从 L2 接续，L1 已折算免做）。** 同日新建旁路速讲轨道 `.claude/agents/nvr-quick-tour.md`（口令「快速过项目/项目速讲」；直接讲解+复述检验；产出只落 `面试备战/项目速讲/`；不推进任务 ID、不写学习文档、复述不算主线证据），AGENTS.md 坐标系已登记。

- 2026-08-21（续·直问模式）：1.7B-3 的坏包路径题由 AI 直讲（学生未复述，不构成掌握证据）：recorder remux 不解位流→坏包原样写入、MP4 结构完好但该样本永久损坏（回放花屏到段内下个关键帧）；feed 走 c:176-177 return 0 静默丢、预览伤害到下一个 IDR 重置参考链才彻底恢复；rtsp 按起始码机械切 NALU 原样转发零感知（客户端自解码自愈）；综合=重连决策者仅 c:157-158 的 err_count，坏包在 IO 层正常不进错误计数；旁路不阻断主链三根柱子（不解析就不出错/错误通道在调用点剪断/失败就地降级丢帧），对比主路错误=可升级（持续性故障值得重连）+连续20次防抖=连续计数器；另有读层错误通道（c:147-150 av_read_frame 失败→break 重连）兜住持续性网络损坏。
- 并行支线（2026-08-20开，08-25更新）：Phase1.5**概念层**B1、B2、B3、B4已通过（L2，4/7单元）。B3（NALU头与类型）证据：0x67字节拆解全对（F/NRI/type）；NALU存在动机两腿超出预期（IP分片任一片丢失整帧报废+slice级流水线降端到端延迟）；定义独立合成（"压缩算法与传输网络间的标准化桥梁"）；Q2双路识别（flag vs &0x1f）首答因果有偏（"码流确定性"），纠偏后学生**独立复述框架**（"差别不在输入，在要输出什么"）并**主动深化提问**"为什么拉流线程只需包级"（答案：三分发三项决策均为包级——路由不看类型/recorder闸门=包级yes-no/feed不挑帧；remux不转码不开箱）；"库解析了但只暴露包级结论、parse_nalus是库内部劳动的自研重做"细节仍由tutor纠偏——定级：**框架独立+细节纠偏，待B4自然复验**；术语纠偏slice≠NALU全部；parse_nalus两分支+回调zero-copy直讲展开（:66-110，:306/:552两消费点）。4卡已落`八股卡片/05_协议.md`。**B4（Annex B+AVCC，08-25收口）证据**：B2尾巴SDP三行首答全对（fmtp/sprop=去起始码裸NALU的base64）；钩子自主挖出定界符机制；纸笔+闭卷切分全对（4/3/3混用圈注、[start,end)左闭右开自主表述、EOF尾段、type回收B3）；三形态识别全对（avcC位级解读超所教=提升池）。**深度滤网08-25强化**：变式（4字节嵌3字节歧义）进行中用户中断重申不深挖AV内部，当场降一句话结论入提升池，滤网固化为逐题过检（AI长期记忆已更新）；B5吸收压缩版直讲（一句话区别/两把尺子结论/avcC三层结论），avcC位级布局留提升池。2卡入05_协议.md（累计6卡），B4用时未报告。**B6（RTP 12字节头+三要素，08-25收口）证据**：钩子超额全对（UDP头8字节4字段+四行能力补充表=块内容地图）；seq/ts区分三情境独立完成（同帧同ts/变帧率ts跳跃/音画同步靠RTCP-NTP）；FU-A三包示例中M/ts/seq取值正确=三要素变化规律独立证据；字节拆解与V/P/X/CC为tutor对照讲解级（08-25用户再校准：拆字节属超线，练习取消，仅留结论级"0x80=版本2的标准头+大端坑"一句话即可——加分内容今后先标层再投入）；术语纠偏一处：ts非绝对时刻，是随机基址+单调的相对时钟，绝对对齐靠RTCP SR。**B9（FU-A）学生同日自主预习全部核心**（FU Indicator/Header结构、S/E/type、还原公式`(FU ind&0xE0)|(FU hdr&0x1F)`、组帧五步），tutor逐项核对无误并对照源码（indicator c:238/M双条件c:247/S 0x80/E 0x40 c:254-256）；项目实然三条：无STAP-A、M=(last&&marker)双条件、ts恒0(c:302,审计层素材)。2卡入05_协议.md（累计9卡）。**B10（RTSP/RTP分工，08-25收口）证据**：B9两道快问——Q1因果链全对（丢分片→NALU拼不回→本帧+依赖帧坏→等IDR，主动复用B1参考链）但"MP4/预览侧卡顿"腿归属纠偏（丢包只在rtsp→客户端腿，三分发另两腿不受影响）；Q2 nal_hdr对、c:247双条件由tutor补结论；钩子两层独立（RTSP无媒体字节+传输单位是包非帧），interleaved第三层与帧-NALU归属由tutor一句话精修；封口题"为什么不都用TCP"**双向反事实独立构造**（全TCP→延迟累积；全UDP→端口与PLAY时机信令丢失会话建不成）。**M-001正式关闭**。1卡入05_协议.md（累计10卡）。**概念层就此收官（B1–B10完成，B11并入实现层）**。B10用时未报告。08-25再规划（用户按秋招线校准后重排）：B9复验并入B10开场；B11取消独立开块、五方法并入实现层P1走读（B11预制文件留作走读材料）；概念层B10后即收官（6/7口径，B11记并入）；剩余概念层投入~65min→~30min，阶段预算≈7h→≈6.5h。主线ID以「当前位置」小节为准（现为1.9），概念块不推进任务ID。块级收口流程：判定+阶段说明→落八股卡→本文件轻量更新→下一块预览。复测按08-21再修订：全部暂缓至项目收尾统一处理（B1两卡24h/72h已到期，仅登记）。
- 1.6C收口摘要：①段1（cleanup_disk）通过：伪代码约80%正确+三处纠偏（scandir第三参NULL未过滤/strstr子串保护当前段/终止条件在for头）；闭卷两题通过（只读100段遍历完退出；反馈式清理三理由=文件大小不一+共享空间动态+瞬时快照）。②段2通过：快照模式（锁内复制锁外计算）口述正确；四类失败账独立完成约90%，**写头失败 waiting_key=0 残留态由学生独立答对**；recording两条件与=防"开段成功但头未写"误报窗口（学生答出）。③**两个新审计发现**：I-05 写头失败后下一个非关键帧经 c:215/226/234 直接重写头并成为段首帧→非关键帧起段窗口（学生机制复述全对）；I-06 磁盘满场景 err_count 被非关键帧 return 0 清零→熔断永不触发→实际表现为录像停摆+每关键帧重试而非重连（修正此前"熔断重连"表述）。④Q2 set_segment_seconds 无锁读竞争边界按学生要求推迟至补强阶段池。
- 1.7C 完成条件已全部满足（2026-08-22）；时序图以口头题等价替代（锁粒度双保证）。
- 1.9 完成条件：主数据流图、三模块输入/输出/状态表、AVPacket/录像段输出上下文/最新JPEG缓存三生命周期、两条失败路径、≥8道why且核心因果无误；**含坏包路径换情境独立抽查**（1.7B 为直讲后复述级证据）。1.9 通过→Phase 1 收口→Phase 1.5（B2 起）/Phase 2（前置概念 2 段 + 代码 6 段）。**1.8 已由 AI 代做完成（2026-08-21，学生豁免）**。

## Phase 进度

| Phase | 状态 | 已完成/预计 | 当前里程碑 |
| --- | --- | ---: | --- |
| 0 架构地基 | 已完成 | 4/4h | 架构图与分层 why 已通过 |
| 1 视频主链路 | **已完成（2026-08-28 收口）** | 30.5h＋1.9系列未报告/35.5h | 1.5约4h、1.6A约5h、1.6B 3.5h、1.6C 4h、1.7A 3h、1.7B/1.7C/1.9系列未报告；1.9-1/T1–T7/V1–V3/简历v5/8why/口述版全通过 |
| 1.5 协议核心 | 概念层已收官；实现层在**定向回补池** | 概念层6/7收官（B11并入实现层;08-25,10卡入05_协议.md,M-001关闭）;概念层用时未报告 | 实现层~3h+审计~0.5h；触发=Phase 2 完成后/临近面试/模拟面试塌点 |
| 2 云 AI + 事件 | **已完成（2026-09-16 收口）** | 约9.4/9.5h（各单元用时未报告按预算中值坐标；09-16 修正旧 4.4h 口径，旧值未含 2.3A/B/2.4） | C1✅；2.1✅（09-03）；2.2✅（09-04）；**C2✅（09-06）**；**2.3A✅（09-15）**；**2.3B✅（09-15）**；**2.4✅（09-16）**；**2.5✅（09-16，闭卷 8/8+V-c，Phase 2 闭合）** |
| 3 Web + 配置 | **已完成（2026-09-20 收口）** | 3.1–3.6 用时均未报告/5.5h（现实预期 7.5–8.5h，待报告后回弹） | 3.1✅09-17/3.2✅09-17/3.3✅09-18/3.4✅09-19/3.5✅09-20/3.6✅09-20（AI 代做+阶段总验收：C1+三场景+闭卷8+变式V+90s复述，三必对全过、两发实然推翻题设、缺陷#26 独立发现、八股07共20卡） |
| 5 V821 概念 | 待办 | 0/3h | — |
| 6 面试 + 证据 | 穿插进行 | 约4/15h | why 题随学习累计 |

校准用时坐标：Phase 1已完成 **约30.5/35.5h（约86%）**（24.5h实际确认 + 1.7B/3.5h、1.7C/2.5h用时未报告按预算坐标；1.8为AI代做计0h），其中1.5按用户回忆约4h、1.6A实际约5h、1.6B实际3.5h、1.6C实际4h、1.7A实际3h（后三项均补报）。顺序主线累计为 **Phase 0的4h + Phase 1的30.5h + C1 0.75h预算 = 35.25/78.5h（约45%）**（总口径2026-09-02修订为约78.5h：Phase 2 6.5→5.5h、Phase 3 9→5.5h；此前 08-22 为约83h）；另有Phase 6穿插投入约4h，计入后总学习量约 **39.25/78.5h（约50%）**，按预算剩余约39h。已完成任务优先使用实际/回忆用时，无报告时标"未报告"且以预算值作坐标；两者都不替代掌握验收。2026-08-17系统修订：以1.5=4h（2.0×）和1.6A=5h（1.67×）为锚点，1.7由6h上调至8h（frame_grabber新API面最大）、1.9由2.5h上调至3h（复测深度），其余Phase倍率落在锚点推导区间内不动；总路线84h→86.5h，并建立“实际用时滚动复核同类预算”规则。2026-08-18补报判读：1.6B实际3.5h=预算5h的0.7×（首次低于预算）、1.6C实际4h=预算3.5h的1.14×（该卡额外含I-05/I-06审计发现与M-008补录）；1.6模块合计实际12.5h vs 预算13.5h（0.93×）——总量符合、B/C分配与预算相反，任务间方差大，L3类倍率暂不整类下调，待1.7A实际样本复核。2026-08-20判读：1.7A实际3h=预算2h的1.5×（该卡实际承载两域边界原则、AVFrame双通道、跨模块生命周期审计等概念负载，超出纯init资源图）；近三任务0.7×/1.14×/1.5×均值约1.1×，预算与实际总体贴合，L3类倍率维持，1.7B预算3.5h不动。2026-08-22机制修订：Phase 2/3 开卡前各增前置概念层（4块×45min=3h：C1回调/发布-订阅、C2条件变量、C3 socket/TCP、C4 HTTP模型），Phase 2 5h→6.5h、Phase 3 7.5h→9h，总口径约80h→约83h；课程地图见Phase2_3前置概念框架.md。2026-08-27登记：1.9 系列（1.9-1、T1–T7 口述复盘、V1–V3 变式、简历条目五轮迭代）用时均未报告，Phase 1 确认用时维持 30.5h 口径、完成度按子块另行呈现；Phase 1.5 概念层（B2–B10）用时同样未报告。**2026-09-02 口径修正**：Phase 2/3 时间盒（09-02 砍伐版）未含交互往返成本（打字作答/AI 纠偏追问轮次/元问题打断）且零实测样本，按历史 L3 倍率 1.5–1.8× 现实预期 **Phase 2 约 7.5–9h、Phase 3 约 7.5–8.5h，总路线约 82.5–85h**（与 08-22 旧口径 83h 几乎重合——砍的内容是真砍，交互往返成本把省时吃回）；正式回弹待 2.1（P2）/3.1+3.2（P3）首卡实测执行，卡内时间盒降级为节奏控制不再当完成承诺。**2026-09-16 收口更新**：Phase 2 按 09-03 ×2 回弹后的 9.5h 口径执行并收口（2.5 用时未报告按 85min 坐标，Phase 2 坐标≈9.4h），顺序主线累计约 **43.9/78.5h（约 56%；按 82.5–85h 现实分母约 51–53%）**；总计划时间模型已同步。

## Phase 1 当前任务清单

| ID | 任务 | 状态 | 通过标志 |
| --- | --- | --- | --- |
| 1.1–1.3 | 三模块 `.h`、输入/输出/状态 | 已完成 | 三张模块表 |
| 1.4 | 三分发数据流与单线程设计 | 已完成 | 数据流图 + why |
| 1.5A | `open_input` | 已完成 | 自主分块 + 闭卷复述 |
| 1.5B | `receiver_thread` | 已完成 | 两层循环和重连图 |
| 1.5C | packet、状态、start/stop | 已完成 | 三分发、公开接口、生命周期与`unref`已收口 |
| 1.6A–C | `recorder.c` | **已完成（L3）** | 状态机/remux/关键帧/fMP4/时间戳/磁盘回收/状态查询/失败边界全闭合 |
| 1.7A–C | `frame_grabber.c` | **已完成（L3，2026-08-22）** | 三卡闭合：初始化资源(✓)+解码-JPEG链(✓)+节流/线程安全/审计表(✓) |
| 1.8 | `gateway.conf.example` | **已完成（2026-08-21 AI 代做）** | 40 key 三方核对 1:1；样例+核对表已交付，学生豁免 |
| 1.9 | Phase 1 闭卷总验收 | **已完成（2026-08-28）**——1.9-1/T1–T7/V1–V3/简历v5/8why 8/8/口述版全过 | 图、表、why、异常追问均≥80% |

## 当前掌握度

| 知识点 | 等级 | 证据/待补 |
| --- | --- | --- |
| 端/边/云架构 | L3 推导 | 能解释算力分层 |
| 主数据流 | L2 复述 | 需继续巩固 RTSP/RTP 角色 |
| 三模块 `.h` 接口 | L2 复述 | 已完成输入/输出/状态表 |
| packet 三分发 | L3 推导 | 能解释单线程同步分发取舍 |
| `stream_receiver.c` 实现 | **L3（1.9 收口升级）** | 1.5全证据+1.9增量：T4三腿消化/计数只接写盘+两处自学发现（+discardcorrupt、probe参数）、T7重连自含+stop顺序逐行一致、Q4 unref反事实drain自推、Q7自愈trade-off四条付出 | 停止延迟讲解后V3复述达标(08-27) |
| `recorder.c` 实现 | L3（1.6A/B/C全通过+1.9复验） | 三卡全证据不变；1.9增量：Q2关键帧twice-proof、Q3 fMP4边界完整复述、V2段首坏IDR强通过、T5/Q8时基分裂签名 | faststart与"去掉+frag_keyframe"两道遗留变式仍开放(收尾统一) |
| `frame_grabber.c` 实现 | L3（1.7A/B/C三卡闭合+1.9复验） | 1.7全证据不变；1.9增量：V1访问集分析法自主构建、Q5状态vs历史+拷贝四后果、Q6单调钟双向跳钟推演 | — |
| 陌生 C 模块高效阅读方法 | L2 稳定复述 | 已形成“整体/依赖→状态→函数地图→实现/失败/why”方法；后续在`recorder.c`检验独立迁移效率 |
| H.264 码流语法基础（GOP/IDR/SPS-PPS/NALU头，概念层B1–B3） | L2 | 双路识别（flag vs &0x1f）为框架独立+细节纠偏级，待B4起始码复验；证据见并行支线行与05_协议.md 4卡 |

## 当前薄弱点

1. （已关闭 08-25，M-001）RTP 承载媒体、RTSP 控制会话的角色区分——B10 双向反事实独立构造通过。
2. V821 应准确表述为”RISC-V CPU 核 + VENC 硬件编码引擎”（Phase 5 验证）。
3. FFmpeg 配置类机制（运行时字符串选项/位掩码/字典）已补课并当场通过变式，属当天新建立概念——随复测暂缓规则累积（M-006/M-007），收尾统一复验。
4. （已关闭 08-27）M-008 clone vs AVFrame——V3 复述达标。
5. 阅读方法框架已在 recorder/frame_grabber 双模块迁移验证；下一迁移场景=Phase 1.5 rtsp_server.c 实现层。
6. recorder 两道遗留变式（faststart / 去掉 +frag_keyframe）未测，入收尾统一复测池。

## Why 题状态

### 已掌握

- （1.9-3 总验收 2026-08-28）八道 why 闭卷 8/8（手册原题，三必对全过；unref 时机与重连自愈两题为总验收新验证）。
- 为什么分端/边/云三层。
- 一次 `av_read_frame` 后 packet 的三个去向。
- 为什么 recorder/frame_grabber 不各自增加消费线程和队列。
- frame_grabber 为什么只保留最新帧。
- frame_grabber_get_jpeg 为什么返回 malloc 拷贝。
- 为什么需要 `av_packet_rescale_ts`。
- 为什么录像只remux不转码。
- 为什么首段和业务切段都选择关键帧边界。
- 为什么使用 fragmented MP4（三个movflags、断电可恢复边界、frag_duration兜底）。
- 为什么输出时基采用 `1/90000`（RTP 90kHz惯例与写死输入假设的重合/失配后果，90倍速变式通过）。
- `cleanup_disk` 为什么以及如何删除最旧录像段（六步算法+反馈式清理三理由+文件名排序假设边界）。
- 为什么节流不能放在解码之前（I/P/B参考链必须逐包维护）。
- 为什么 recorder 挑关键帧而 frame_grabber 节流不看帧类型（两域输出边界原则）。
- 为什么 g_dst_frame 两步释放而 g_dec_frame 一步（AVFrame 内存双通道：登记在册 vs 裸挂）。
- 为什么 send 一次要用 while 循环 receive、而编码侧只 receive 一次（0/1/N 帧 vs JPEG 帧内一帧一包）。
- send/receive 的 EAGAIN 两侧语义（receive=输出空常态出口；send=输出未排空拒收、契约要求重发、本代码不重发真丢一包但几乎不可达）。
- 为什么全链三次拷贝（clone/memcpy/malloc）——跨所有权与线程边界即拷贝断开；输入 pkt 纯借用不建不写不毁。
- pts 拷贝在本代码无下游消费者（三层证据：管线惯例传/缓存只拷 data+size/时间来自发送节奏与墙钟）。
- 为什么坏包不触发重连而 recorder 写错误会（不解析就不出错+err_count 防抖+持续故障不对称；复述级，1.9 换情境独立抽查）。

### 待巩固

- （补强阶段池）`set_segment_seconds` 锁内写 vs `write_packet` 无锁读的竞争边界：实践可跑、C规范层面属数据竞争——主线结束后系统讲 C 内存模型再裁决。

## 学习记录

- 2026-08-08：完成 Phase 0；完成三模块头文件与接口认知。
- 2026-08-10：完成三分发数据流图；通过单线程同步分发 why 题。
- 2026-08-11：复核项目、教学 Agent 和详细学习路线；确认当前任务仍为 1.5。
- 2026-08-12：确定通用阅读方法“AI 脚手架→学生追踪→AI 纠偏→闭卷复述”；将 Phase 1 明确为三个核心 `.c` 的纵向精读，并精简进度文档。
- 2026-08-12：1.5A `open_input` 通过基础验收；能说明三个参数、打开/探测/选流/交付阶段及失败清理。因时间安排，先读文件底部四个公开接口，再回到 1.5B。
- 2026-08-12：`start/stop/get_status/set_segment_seconds` 基础验收通过；能区分生命周期入口/出口、状态查询、配置转发，理解 `g_running=0 → pthread_join → 释放资源`，并区分“API返回成功”和“业务已连接”。
- 2026-08-12：新增`Phase1任务验收.md`，为1.5/1.6/1.7建立9张可执行任务卡；总计划只保留概要和链接。
- 2026-08-12：澄清 Phase 1/2/3 的定位：Phase 1 是边侧视频数据主链路，Phase 2 在 JPEG 旁路上形成 AI 事件与报警，Phase 3 提供 Web 展示、回放和配置热更新；本次仅建立阶段关系，尚未计入 Phase 2/3 掌握度。
- 2026-08-12：根据“嵌入式 Linux 音视频应用”求职方向拟定 Phase 2/3 压缩策略：Phase 2 重点保留发布者线程回调、条件变量报警线程和资源所有权，其余云 API/JSON/SQLite CRUD 快速理解；Phase 3 保留 MJPEG、HTTP Range、socket 生命周期和配置原子保存/热更新，普通 JSON API 与前端略读。当前 Phase 与验收状态不变。
- 2026-08-12：正式更新唯一事实源计划：Phase 2 由 7.5h 压缩为 3h，Phase 3 由 8.5h 压缩为 4.5h，总路线由 63.5h 调整为 55h；为每项标明 L1–L2/L3 深度、略读范围、验收证据和岗位相关性原因。当前仍从 1.5B 接续。
- 2026-08-13：1.5B `receiver_thread` 通过：闭卷复述外层连接/内层读包两层循环、三个 break 落点（114=退出线程、149/157=断开重连）、连接失败 continue 重连、两处 unref 兜底；纠正 continue 语义与“内层只派发不转 JPEG”的表述。
- 2026-08-13：补齐全部剩余必修及可选任务卡：Phase 1新增1.8/1.9，建立协议、云AI/事件、Web/配置、V821/面试证据、可选扩展手册与全路线索引；按真实源码复核原指引，加入“规范/设计应然—源码实然—工程结论”审计。学习进度未前移，仍从1.5B接续。
- 2026-08-13：审计一次由其他模型执行的1.5B教学对话：其脚手架、局部源码纠偏和控制流讲解总体有效；真实缺口是内层循环尚未回答、主路/旁路为何采用不同失败策略尚未形成因果解释。末尾提前把“两处Packet unref为何存在”带入1.5C，属于越界。缺少五种固定格式本身不再作为阻塞理由；当前因核心内容尚未收口，仍为1.5B进行中。
- 2026-08-13：按用户新的效率偏好更新教学Agent：任务卡改为诊断题库，Tutor可依据独立口述、源码追踪和变式回答自主判定掌握并压缩/合并产出；不再以交齐图表或固定80%为硬门槛。默认教学顺序固定为“整体数据流与职责→头文件/库/关联模块→关键状态→函数地图与主流程→实现细节→错误后果→技术选型why”。仍禁止带着关键因果错误前进或无说明越界深讲。
- 2026-08-13：1.5 `stream_receiver.c` 收尾：1.5C packet 生命周期通过（音频过滤、三分发顺序、170/172 两处 unref 兜底）；stream_receiver 主链路达 L3；1.6A `recorder.c` 待开始（学生已自发读 close_segment）。
- 2026-08-13：建立Phase 1后续难度坐标：1.6难在录像状态机、fMP4/时间基与封装资源；1.7难在压缩/原始/JPEG格式变化、FFmpeg send/receive与共享缓存；1.8是配置契约、可部署性和安全交付任务；1.9负责综合收口。本次仅澄清路线，当前任务仍为1.5C。
- 2026-08-13：校准推荐用时进度：Phase 1已确认完成1.1–1.5B，对应6.5h，当前处于预算0.5h的1.5C，因此位置为6.5–7/21h；完成1.5C后进入1.6A。总路线同时区分顺序主线10.5/55h与含Phase 6穿插投入14.5/55h，避免把推荐预算误写成真实计时。
- 2026-08-13：用户确认1.5C已完成；Phase 1推进到7/21h（33%），`stream_receiver.c`任务1.5全部收口。唯一当前任务更新为1.6A `recorder.c`录像状态机、remux与关键帧切段；顺序主线累计11/55h，含Phase 6穿插投入约15/55h。
- 2026-08-14：完成gateway侧11个`.c`的函数注释审计，为缺少说明的函数补充一句话职责，并纠正少数与源码不一致的旧注释；未修改代码行为，也不改变1.6A学习进度。
- 2026-08-14：1.6A完成阶段性阅读。学生独立确认`recorder`是同步下游、输入Packet为借用，完成结构体字段分类与公开函数主流程；能判断“到时但非关键帧继续写旧段”“切段关键帧进入新段”“关键帧起段解决参考帧依赖”，并追到新段打开失败后的`ofmt=NULL/waiting_key=1/返回-1/下个关键帧重试`。纠偏点：不能把`open_segment/close_segment`都视为叶子函数；`waiting_key`不表示正常录像期间等每个下一关键帧，正常切段由`header_written && is_key && elapsed>=segment_seconds`组合条件直接决定。任务暂判部分通过，进度不前移。
- 2026-08-17：1.6A通过。学生在间隔复测中恢复连续写失败计数、超过阈值后跳出内层循环、循环外Packet兜底释放和外层重新`open_input`；经提示补齐重连前`g_ic=NULL/avformat_close_input`。能区分到时但无关键帧时继续写旧段与显式`waiting_key`门控，并完整口述转码所需的解码上下文、原始YUV帧、编码上下文和新Packet，完成remux反证。下一任务为1.6B。
- 2026-08-17：按真实学习速度重新校准路线用时。用户报告1.6A实际约5h，1.5A–C合计约为原2h预算的两倍；后续L3源码/协议任务按约1.7–2倍、L1–L2略读任务按约1.4–1.7倍估算。Phase 1调整为33h，整条必修路线由快速口径55h调整为84h；Phase 2/3仍保持内容压缩，不恢复低收益逐行精读。
- 2026-08-17：应用户要求做系统完整用时修订（锚点：1.5实际4h、1.6A实际5h准确）。以锚点倍率（L3源码任务1.67–2.0×）复核全部任务：1.7A–C由6h上调至8h（frame_grabber为三模块中新FFmpeg API面最大者，原值与锚点节奏矛盾），1.9由2.5h上调至3h（复测深度证据）；1.6B/1.6C/1.8及Phase 1.5/2/3/5/6维持不变（倍率均落在推导区间）。Phase 1由33h→35.5h，总路线84h→86.5h，剩余约64.5h；新增滚动校准规则（实际用时连续两次落在区间外时按新均值整类调整）。手册任务表时间列与总计划统一为同一口径。
- 2026-08-17：教学体系升级——按用户要求在教学铁律13任务卡清单新增第一项“任务的目的与意义（为什么学/在版图中的位置/学完能做什么/前后衔接）”，AGENTS.md与nvr-tutor.md同步修改，1.6B任务卡已补“任务目的与意义”段。防止学生不知目的而迷失。
- 2026-08-17：1.6B阶段性进展（会话因额度收尾，任务保持进行中）。①box概念输入：moov=全局目录/mdat=正文/moof=章节小目录；fMP4开头写空moov+每关键帧fragment落盘；谨慎表述”提高中断可恢复性≠零丢失”（断电瞬间未完成fragment仍会损坏）。②学生提交`open_segment`前半资源链：输出上下文→输出流→IO句柄；亮点=自主推导”AVStream生命周期归上下文管，close无需单独释放”（正确的所有权判断）。③纠偏M-005：把`if(!(flags&AVFMT_NOFILE)) avio_open`读成”是NOFILE，muxer自己管io”——实际NOFILE=muxer声明不需要调用者文件IO，mp4无此标志才进入avio_open；close侧c:56-57对称守卫。④`close_segment`对答案：学生猜对”先closep后free”逆序骨架，补漏av_write_trailer（header_written守卫）与状态复位（waiting_key=1两头置值呼应M-004）。⑤goto fail统一出口覆盖三种部分失败（AI讲解）。遗留：2道闭卷题、movflags注释vs实际审计、out->size统计疑点、clone/rescale/write/free、时间戳演算。
- 2026-08-17：1.6B资源生命周期半边收口+agent要求升级。①闭卷题1通过：avio_closep先于free_context——学生独立答出”pb挂在上下文里，free后访问路径本身悬垂”，补FFmpeg所有权约定（free_context不关调用者开的IO）。②闭卷题2（M-005变式）：标志存在→条件假→不执行→pb保持NULL全对，M-005关闭；学生诚实提出”不懂RTP是否要先学”——借机教”陌生名词题先抽象化检验考的是结构还是名词”的通用技巧，零协议知识完成迁移。③术语补课：守卫=包住操作的if条件；对称守卫=open/close同条件配对，保证资源配对不出错（不对称=对NULL关崩溃/未开文件泄漏）。④按用户要求扩充教学铁律13：任务目的与意义须结合背景写充实（四层：任务解决什么问题/项目背景与选型动机/岗位面试背景与筛人逻辑/学完能力变化与衔接），AGENTS.md与nvr-tutor.md同步。⑤movflags审计任务已发布（注释vs实际差异、clone→rescale→write→free、out->size疑点），待学生提交。
- 2026-08-17：教学体系扩展——新增`.claude/agents/nvr-knowledge-tutor.md`知识型教学Agent（六阶段45分钟闭环：问题钩子→最小模型→样例操作→闭卷重建→变式纠偏→重逢绑定＋次日快问快答；开块前必备2正常+2陷阱字节样例、类比锚点、RFC定位清单、复测题库；知识块只认证L2、不推进任务ID、知识块通过≠P卡通过），AGENTS.md路径坐标系已同步登记。用途：Phase 1.5四组协议概念提前准备（推荐顺序：组二H.264语法→组四RTP→组一RTSP→组三SDP）及后续Phase5/Phase2-3概念型内容。当前任务ID不变，仍为1.6B（movflags审计待提交）。
- 2026-08-18：1.6B通过，推进到1.6C。①movflags机制块：学生暴露两处机制级误解（标志串当算术值、movflags当"帧头部"），当场纠偏并按用户要求固化为AGENTS.md教学铁律21（前置概念声明）与22（标识符词源解析），同步写入AI长期记忆（当天3次被点名，含时间基vs封装格式未前置）。②movflags注释vs代码审计：集合一致、语义方向正确（default_base_moof措辞可打磨）；"注释与代码冲突以谁为准"用三层证据链（注释声称→代码决定→库语义/产物事实）作答。③标志顺序题：学生自主推出"开关配置无顺序影响"（不同标志按位或可交换；仅同标志+/-冲突时左到右生效）。④clone/rescale/write/free：clone=浅拷贝（新壳+引用计数+1），原pkt归receiver读循环三分发共读；write_frame成功拿走引用并掏空包。⑤时间戳演算：90倍速变式通过（pts数值不变、含义从1/1000变1/90000刻度、播放快90倍）；"解码速度变快"措辞已归位为"时间轴刻度解释错→呈现节奏错"。⑥**审计确认真实bug**：`recorder.c:262`在write_frame掏空packet后读`out->size`→`bytes_written`恒0→`bitrate_kbps`恒0→Web"当前段码率"显示恒0（学生独立推完整链，已记入代码质量TODO，修复留给动手任务）。另确认时间基未传入recorder是脆弱假设（rtsp_server那路传了tb，recorder没传）。1.6B实际用时3.5h（2026-08-18补报）。
- 2026-08-18：1.6C通过，recorder模块三卡闭合达L3，推进到1.7A。段1：cleanup_disk伪代码约80%正确，纠偏三处（scandir第三参NULL=未过滤、过滤在循环手动；跳当前段=strstr(cur_path,name)子串包含；终止条件在for头=i<n且空间仍不足）；闭卷①只读100段：遍历完退出+问题未解决+下次切段/重启再试；闭卷②反馈式清理：时长同但码率异、TF卡共享空间动态占用、statvfs瞬时快照。暗题确认"删目录下所有mp4无前缀过滤"（学生提议并建立档案后续改进清单I-01~I-04）。段2：快照模式口述正确（锁内复制、锁外计算、快速释放）；四类失败账约90%独立完成，**写头失败waiting_key=0残留态独立答对**，纠偏"fail块释放的是输出上下文、avio_closep为防御性守卫、状态值靠调用点语境保证"；recording两条件与的误报窗口答出。**新审计发现I-05**（写头失败后非关键帧经215/226/234重写头成段首帧→非关键帧起段窗口，学生机制复述全对）与**I-06**（磁盘满时err_count被非关键帧return 0清零→熔断永不触发→表现是停摆重试而非重连，修正旧表述）。Q2无锁读竞争边界按学生要求入补强阶段池。AGENTS.md新增铁律23（子阶段收口说明，应用户要求）。1.6C实际用时4h（2026-08-18补报）。

- 2026-08-18：1.6C收口后复习补录：学生提出"写头和关键帧没有直接关联，所以写头失败后可以在下一帧到来就进行写文件头的修复"——前半对（写头内容来自SDP面codecpar，确实不挑帧），结论错（c:234块补写头与首帧写入绑死，需要等IDR的是段首样本不是头；该"修复"正是I-05分支A的现状行为）→记M-008，当场讲解纠偏但学生未复述。"写头时机与首帧闸门解耦"设计题（新中间态与c:226交叉+close_segment一步复位为何更便宜）按学生要求推迟至补强阶段池，与set_segment_seconds无锁读同池（池内现2题）。当前ID不变，仍为1.7A。
- 2026-08-18：用户补报实际用时：1.6B=3.5h、1.6C=4h。时间坐标更新：Phase 1完成21.5/35.5h（61%），顺序主线25.5/86.5h（约29%），含Phase 6穿插约29.5h（约34%），按预算剩余约57h。1.6B为首个低于预算的任务（0.7×），1.6C略超（1.14×，含额外审计内容），模块合计0.93×；L3类倍率暂不调整，待1.7A样本复核。同步更新Phase1任务验收.md任务表中1.6B/1.6C两行用时标注。
- 2026-08-19：秋招策略路线修订（用户确认）——学习目标定为"完整讲清一条链路+技术选型+其中问题与解决方法"，不投入超出当前积累与时间预算的高难度源码审计（非能力问题，是时间窗口下的优先级）。执行结构：①基础概念先行（Phase 1.5概念四组+Phase 5 V821概念并入）；②Phase 1/2/3按流程级深度完成（逻辑/模块功能与联系/错误退出机制，Phase 2/3维持手册压缩版口径）；③Phase 1/2/3之后进入总结整理（技术选型/完整链路/问题与解决）+模拟面试实测，实测塌点决定是否回补深钻。Phase 1.5按三层处置压缩：概念必做~6.5h/实现压缩~3-4h（P1全程+P2/P5浅层）/审计降级~0.5h（五项缺陷一句话结论，作为"项目问题与改进"面试素材），预算16.5h→10h；Phase1.5手册已加处置修订段，总路线86.5h→约80h（总计划待Phase 1收口时统一同步）。"问题与解决"素材三源明确：失败路径（主动学）+已知缺陷清单（结论级占有）+改进点设想。当前任务ID不变，仍为1.7A。
- 2026-08-20：1.7A通过（流程级），推进到1.7B。①流程口述两处方向纠偏后内化：send_packet是解码器入侧（学生原表述"输出队列"方向反）、节流在emit开头挡缩放+编码+缓存、feed对每个包无条件送解码（参考链必须逐包维护，学生以I/P/B依赖独立论证）。②两域输出边界原则独立合成：recorder切段挑关键帧vs grabber节流不看帧类型，压缩域边界须落参考链重置点/原始域每帧完整；闭卷对比题通过，另纠"CPU取舍解释为何节流、不解释为何不挑帧"。③AVFrame双通道内存所有权：V1变式（改用av_frame_get_buffer→登记在册一步free，手动av_freep会双重释放）与V2变式（对调两行→仅126=泄漏、再跑125=NULL解引用UB→SIGSEGV分层作答）均通过；纠偏"泄漏≠悬垂指针"术语。④init七阶段/早退原则（c:51/53资源未建直接return，清理义务与已获资源对等）/g_cfg=副本（热更新钩子：set_target_fps改私有快照字段）/deinit清g_jpeg加锁的g_lock三角色（写=emit@receiver线程、读=get_jpeg@web+cloud线程、毁=deinit）。补讲：尺寸阶段依赖链（g_dst_h=(1080×640/1920)&~1=360，偶数对齐因YUV420色度横竖双维减半采样）。⑤跨模块生命周期核实：frame_grabber_init在stream_receiver.c:120-130（!g_fg_inited守卫防重连重复初始化）、deinit在stream_receiver_stop(c:211)且在pthread_join之后；main.c停机顺序web→cloud→receiver→alarm→event_bus，当前生命周期内deinit锁防御性不实争用（三层证据结论）。八股卡片04_frame_grabber.md建立（2卡：两域边界原则/双通道释放）。1.7A实际用时3h（2026-08-20补报，预算2h的1.5×）。
- 2026-08-20：备考体系建设会话（未推进任务ID，主线仍1.7B待开始）。①新增`nvr-knowledge-tutor`知识导师agent（六阶段45min闭环：问题钩子→最小模型→纸笔样例→闭卷重建→变式纠偏→重逢绑定＋次日快问快答；知识块仅认证L2、不推进ID）。②建立`Phase1.5概念知识框架.md`：B1–B13十三块（组二H.264→组四RTP→组一RTSP→组三SDP），每块五件套（定义/动机/结构/落点/变体）与八股卡字段一一对应，块收口直落`八股卡片/05_协议.md`（学习即八股管道）。③建立`项目知识地图与面试索引.md`：7类线程事实底座、四链路知识点[L]/[V]/[P]标注、16条选型why清单（9✅）、12个Linux技能域×项目证据、面试题六类弹药现状。④按用户提供的《项目八股准备操作手册（video2lcd跑通版）》建立`面试备战/`体系：00索引、01项目骨架（D1–D6板端必采数据清单＋缺陷清单＋身份话术要点）、`题库/07宪法`（项目八股≠通用八股；P0≤18练L3/P1≤15练L2换题不扩量；问答分离；双层体系=积累层卡片＋冲刺层题库；与video2lcd分工：共有题主答video2lcd、本项目主答协议/FFmpeg/NVR业务；[数据待补]过渡规则，D1–D6绑定真机部署任务）。⑤P0×18/P1×15冻结；首批15题问答卡生成（arch7＋rec8，行号逐条grep验证）；策展首收割：arch-08断流重连升P0、rec-10（open_input六阶段）/rec-11（四类资源+goto fail+closep所有权）入P1、web-01降P1、proto-04出、evt-05并入evt-03、rec-08并入rec-06。⑥新增`nvr-bagu-curator`八股策展agent（阶段收口触发：扫描why/审计发现/纠偏模型→价值过滤→增量落卡→升级建议；冻结权在用户）。遗留：用户待审首批答案（重点arch-08姿态、rec-11 goto话术）；1.7三卡收口后第二次收割＋auditor附录C首审；总计划预算待Phase 1收口同步（86.5h→约80h）。
- 2026-08-21：1.7B 按用户学习-休息模式拆三段×60min（复测两题/04卡延后决定与并行会话记录一致，未重复）。**1.7B-1（feed 精读，c:172-187）通过**：①前置课=send/receive 推拉模型+两侧 EAGAIN 语义差异（send=输出未排空拒收、receive=输出空没货），以 V4L2 非阻塞 DQBUF 迁移；②0/1/N 帧三情境独立构造全对（SPS/PPS 不吐帧/B帧重排序/积压放行）；③纠偏两处：send-EAGAIN 方向反（官方契约"读空输出后包应重发"，本代码不重发→丢包→参考链断到下个IDR；每次排空故几乎不可达，三层证据结论，契约已查 ffmpeg doxygen）；返回值消费者（feed 恒返 0 且 c:164 无人接=旁路设计上静默降级，对比 c:157 recorder err_count 熔断）；④收口追问通过（空包传播到 rtsp_server、丢包由 c:171 归还）。1.7B-2 待开始。
- 2026-08-21：**1.7B-2（emit 精读，c:135-169）通过**（用户"直接讲"模式收口，铁律7）。①前置课：planar 三平面/data-linesize（V4L2 YUYV packed 对照）、sws_scale 一次调用=缩放+换格式、YUVJ420P 的 J=full range（一句话）；②引导四问独立作答全对（无参读全局 g_dec_frame@拉流线程/两 pkt 辨析借用 vs 私有/JPEG intra 一帧一包故不循环/nb-realloc 防 NULL 覆盖=泄漏+预览断供）；③纠偏 pts：三层证据——拷解码帧演示时间戳属管线惯例，但 c:163 缓存只拷 data+size，pts 无任何下游消费者（MJPEG 靠发送节奏、cloud_ai 用墙钟），学生原答案假设了不存在的消费者；讲解级未复述，挂 1.7B-3 闭卷抽查；④tutor 直讲六对象全链四元组表（H.264 pkt→g_dec_frame→g_dst_frame(YUVJ420P 640×360)→JPEG pkt(emit 私有)→g_jpeg(memcpy)→get_jpeg buf(调用方所有)；三次拷贝交割所有权：clone→recorder、memcpy→g_jpeg、malloc→get_jpeg），未单独闭卷，1.7B-3 总闭卷覆盖。1.7B-3 待开始。
- 2026-08-21：**1.7B-3（闭卷验收）通过，1.7B 整卡闭合（三段×60min模式）**。①全链拓扑闭卷正确：六数据节点+解码车间（变换器非数据节点）+五箭头变换标注全对；trick 题未答由 tutor 补讲（输入pkt=纯借用+FFmpeg API 写数据、代码只搬所有权）。②pts 纠偏闭卷复述完整，正式关闭。③坏包路径作答正确且结构化（remux 延迟失败/IDR 恢复点/parse_nalus 机械切分"不解析就不出错"/防抖+持续故障不对称/c:157-158 准确）——**因并行会话此前已直讲同内容（见上方08-21续条目），记复述级证据，1.9 换情境独立抽查**。手册五项必交产出全覆盖；卡级同步已执行（手册/progress/索引+八股卡04新增4张：0/1/N 与编码侧不循环、EAGAIN 两侧、三次拷贝所有权、pts 三层证据）；档案证据与策展第二次收割按既定批量留 1.7 三卡闭合。1.7B 用时未报告。下一步 1.7C。

- 2026-08-21（1.8 AI 代做）：用户评估 1.8 面试直接相关性低后决定由 AI 完成交付物、学生时间只投面试相关主线。已完成：①`edgefusion_gateway/config/gateway.conf.example`（40 key 全覆盖、分组注释、默认值与源码一致、敏感字段留空、热更新键标注）；②`.claude/learning/1.8_配置契约核对表.md`（结构体/conf_load/format_value 三方 40:40:40 完全 1:1；"可配置≠已消费"清单：mqtt_* 全组/ai_max_queue/rtsp_sub_url 解析保存但业务零消费；审计发现：conf.c:196 注释与 c:218/235/236 代码冲突——敏感字段实际在 format_value 白名单内，conf_save 会把 token/密码写回落盘，Web GET 回显已脱敏 web.c:608-620）。学生投入 0h，Phase 1 学生剩余预算调整为约 5.5h（1.7C 2.5h + 1.9 3h）。板端真实加载验证保持"单独标记、可选"。
- 2026-08-21(复测暂缓修订):用户要求保证主线进度——**所有到期复测(24h/72h八股卡、M-xxx台账题)暂缓执行,项目最终收尾(Phase 6/总结)统一批量处理**;AGENTS.md/nvr-tutor.md 开场流程与八股README已同步此规则。开场只登记到期卡数量,不出题、不降级卡片状态。
- 2026-08-22：**1.7C 通过，1.7 三卡闭合，frame_grabber 模块收口达 L3，推进到 1.9**。①四问：节流饿死变式独立推出（每帧更新基准→恒丢）/debounce 标签纠偏/单调钟✅/锁外理由纠偏（判定式=有无第二个线程碰）/撕裂读✅+补 UAF 半边（realloc 搬家 free 旧块=悬垂）。②收尾三件套：四变量审计表高质量（init 线程归属准/**L206 锁外读独立发现**→TODO 第⑨项/精修三格：target_fps 读写不对称、L47 无锁良性、deinit 主线程 join 后）；闭卷题三问全对（240ms<1000ms 丢/第19帧恰在 T0+1000ms 放行/只改门槛不动基准）；时序图以口头题替代："锁划得小"与"②根本不是锁（深拷贝断别名）"双保证自主推导。③模块收口：八股卡 04 +2 张（累计 8 张）、档案看板升 L3+4.3 补登 1.7B/1.7C 证据、手册/索引/总计划同步。④本会话另完成：复测暂缓规则固化、Phase2（6 段）/Phase3（8 段）分段预规划、铁律 18 补"验收情境不越界"、分段模式推广确认。1.7C 用时未报告。⑥**auditor 模块首审(08-22)**:判定"支持 L3(有保留确认)"——行号抽验全对(init@receiver_thread c:126/三分发顺序/L206 锁外读/get_jpeg 范围);证据缺口如实=坏包路径复述级+feed 两漏项已补讲未复述;1.9 建议抽查=坏包换情境+锁迁移变式(把 g_last_emit_us 也搬进 g_lock 能否消除 L206 竞争,考双保证迁移)。
- 2026-08-22（前置概念机制建立）：应用户要求为 Phase 2/3 建立“开卡前前置概念层”机制。新建 `Phase2_3前置概念框架.md`：三层分流（①Phase 1 已建概念标注迁移点复用；②抽象机制类缺口设 45min 独立概念块——C1 回调/发布-订阅、C2 条件变量、C3 socket/TCP 生命周期、C4 HTTP 请求响应模型，走 nvr-knowledge-tutor 六阶段闭环、L2 认证、落八股卡 06/07（随块新建）；③领域惯用机制/命名走卡内铁律21/22 不占段）。Phase2/Phase3 手册已加 2.0A/B、3.0A/B 行并同步 08-17 校准时间口径（两手册原时间列为 08-13 快速口径）；预算 Phase 2 5h→6.5h、Phase 3 7.5h→9h、总路线约80h→约83h；总计划/索引已同步。主线 ID 不变，仍为 1.9。
- 2026-08-23（会话尾·方向确认）：①**1.9 与面试简历合并推进**（三段制：作图/异常推演+auditor两道换情境抽查/8why+简历项目条目初稿+1分钟口述）；②顺序建议获用户认可：1.9→**投递准备包**（半天：简历定稿+挑1-2个TODO修复（码率恒0一行修/L206锁内快照）+板端跑通取D1-D6数据）→Phase 1.5（B2起,10h时间盒纪律）→Phase 2（C1/C2前置概念+6段）→Phase 3（C3/C4+8段）→5→6；理由=协议是音视频岗筛选题+Phase1钩子趁热；③项目含金量定位=校招③档（深度剖析+能讲why+发现缺陷）,升④靠板端实证+自己的改动；④任务量/难度对比表交付（三模块27.5h预算 vs 2+3合计新口径15.5h;难度1.6★★★★★/1.7与1.5★★★★/2与3★★★）。**用户宣布后续重心=Phase 1.5/2/3+收尾与面试表达**。
- 2026-08-23（Phase1.5预制包）：应用户要求批量预制概念层 **B2–B13 前置内容**——新建 `.claude/learning/Phase1.5预制包/`（README+12块文件，每块固定结构：开块审查清单/任务目的四层/铁律21前置概念声明/铁律22词源/类比锚点/五件套草稿/字节样例2正常+2陷阱/六阶段执行卡/快问快答库/通过判定与落卡/RFC定位）。全部落点行号 08-23 对照 `rtsp_server.c`（610行）与 `stream_receiver.c:135/168` 重新核实在案；字节样例取自项目真实缺省 SPS/PPS（base64 手解：SPS 15B `67 42 C0 1E…`、PPS `68 CE 3C 80`）；README 另固化审计层五项缺陷一句话结论+源码锚点速查表+三处附加观察（deinit c:597-598 重复行/控制连接先设非阻塞又恢复阻塞 c:380-383/MAX_CLIENTS 未用）。开块流程固化为：读块文件→审查清单核实→补充→开教；框架进度表已修（B1 ✅ 08-20,2卡）。主线 1.9 状态不变（若已在并行会话完成,下次开场同步）。
- 2026-08-23（Phase1.5二次精简+审查）：①**提问阶梯固化**（用户确认"先位置/作用→再机制→最后设计权衡",四级 L1观察→L2作用→L3机制→L4权衡入预制包README;B2按阶梯重写、B9/B10钩子补铺背景）;②**秋招深度审查**（判据=校招通用层+自研句追问层;B6位级默画/B4 EPB/B12定界对比降【加分】）;③**二次精简**（用户明确主战场=通用Linux应用八股,AV只留「秋招高频或项目自研句必答」,点名B7/B13类不花时间）:概念层13块→**7个学习单元≈4h**（B2含SDP收尾/B3 30min/B4含AVCC 40min/B6合并RTP三要素块/B9 35min/B10 30min/B11 20min）,B5/B7/B8/B12/B13取消独立开块、核心一句话并入、文件留提升材料横幅;实现层P1+P5合为一次点播时序走读~2–2.5h;**Phase1.5总预算10h→约7h**。已同步:预制包README状态表与块横幅、Phase1.5手册处置修订段、概念框架进度表。④B2已由并行会话收口通过（见上方并行支线条目）,05_协议.md累计3卡,当前块B3。⑤上下文预警阈值统一200k（config.json两参数同值）。

- 2026-08-23（并行支线·B3收口）：**B3 · NALU头与类型通过（L2）**，概念层累计3/7单元（二次精简口径）。①证据：0x67拆解全对（F/NRI/type）；NALU存在动机两腿超出预期（IP分片任一片丢失整帧报废+slice级流水线降端到端延迟）；定义独立合成（"压缩算法与传输网络间的标准化桥梁"）。②Q2双路识别（AV_PKT_FLAG_KEY vs nal[0]&0x1f）三轮演进：首答因果有偏（"码流确定性"）→纠偏（数据形态/问题粒度/快递开箱）→学生独立复述框架并主动深化提问"为什么拉流线程只需包级"（答案：三分发三项决策均为包级——路由不看类型/recorder闸门yes-no/feed不挑帧）→"库解析了但只暴露包级结论、parse_nalus=库内部劳动的自研重做"细节仍由纠偏给出——定级**框架独立+细节纠偏，待B4起始码自然复验**。③术语纠偏slice≠NALU全部；parse_nalus两分支+零拷贝回调直讲（:66-110，:306/:552两消费点，:122掩码，:118 sdp_collect_cb只收首份带!g_have_sps守卫）。④4卡入05_协议.md（B3卡要点⑤=三分发包级决策+自研重做话术）。教学偏好沉淀入AI长期记忆：深度滤网、提问阶梯、草图骨架。**下一块B4（含AVCC，40min，先读预制包）**，另有B2尾巴「SDP节目单三行」2min待补。主线1.9不变。

- 2026-08-25（并行支线·B4收口）：**B4 · Annex B+AVCC 吸收通过（L2），概念层4/7**。①B2尾巴SDP三行过（fmtp行/sprop=去起始码裸NALU的base64）；②钩子自主挖出定界符机制，纸笔+闭卷切分全对（4/3/3混用、EOF尾段、[start,end)左闭右开自主表述、type回收B3）；③三形态识别全对（avcC位级解读超所教=提升池）；④**深度滤网强化**：变式（嵌套歧义推演）中用户中断重申"不深挖AV内部、主战场=秋招Linux应用"，当场降一句话结论入提升池，滤网固化为逐题过检（长期记忆已更新）；B5吸收压缩版直讲，avcC位级布局留提升池。2卡入05_协议.md（累计6卡），B4用时未报告。**下一块B6（RTP头三要素）**。另（同日追加）：应用户要求将 avcC 逐字节结构（ISO 14496-15 口径）以**提升层卡**入库 05_协议.md——学生自主整理、tutor 逐项核对无误；核对时发现 `rtsp_server.c:72` 的 `num_sps=data[4]&0x1f` 与标准布局（numSPS 第 6 字节）**疑似差一位**，对规范 avcC 输入会静默提取不到参数集，与源注释"偏移待审计"互证，登记为审计层线索（05_协议.md 源码行已记录）；累计 7 卡。

- 2026-08-25（并行支线·B10收口+概念层收官）：**B10 · RTSP/RTP分工通过（L2），M-001关闭，Phase1.5概念层收官**（B1–B10完成，B11并入实现层，累计10卡）。①B9快问：Q1因果链全对（复用B1参考链）+腿归属纠偏（丢包只影响RTSP客户端腿，三分发隔离复习）；②钩子两层独立（RTSP无媒体字节/单位是包非帧）+interleaved第三层与帧-NALU归属tutor精修；③封口题"为什么不都用TCP"**双向反事实独立构造**；④泳道图（OPTIONS→DESCRIBE/SDP→SETUP/端口对→PLAY→RTP开闸→TEARDOWN）为必答产出。B10用时未报告。**支线概念层全部完成；下一步=主线1.9**。

- 2026-08-25（并行支线·B6收口+B9预习）：**B6 · RTP 12字节头+seq/ts/M三要素通过（L2），概念层5/7**。①钩子超额全对（UDP头8字节4字段、四行能力补充表=块内容地图）；②seq/ts区分三情境独立完成、FU-A示例三要素取值正确；③字节拆解（80 E0 00 05…）与V/P/X/CC为tutor对照讲解级；④纠偏：ts=随机基址相对时钟非绝对时刻（绝对对齐靠RTCP SR映射NTP）；⑤**B9 FU-A 学生自主预习全部核心**（结构/还原公式/组帧五步）核对无误+源码对回（c:238/243/247/254-256），项目实然：无STAP-A、M双条件、ts恒0；B9降为~15min复验。2卡入05_协议.md（累计9卡）。B6/B9用时未报告。**下一块B9压缩复验→B10**。

- 2026-08-26（主线·1.9-1）：**1.9-1 通过（闭卷作图+三模块表；交互选择题模式首次使用——用户要求点击式问卷，判定=自由回忆+再认合并）**。①三分发图口述：Q1 链路全对（Phase1.5概念反哺迁移成功）；Q2 三去向读写性质正确；Q3 六节点+节流位置+解码循环/编码单次正确；Q5 四动词+夺引用理由正确。②tutor 补三处：Q4 g_jpeg 读者（web/cloud两独立线程、get_jpeg malloc拷贝调用方free——Phase1已学内容空白，1.9-2复查）；D=fMP4段文件漏标；H→I包级转发+网关对下游为RTSP server。③三模块表3轮11题选择全对（I-05链/closep双理由/fg挂钩/线程归属/锁判定式/主旁路策略/「消费者队列」干扰项）。1.9-1用时未报告。**复盘修订（08-26同日）**：用户发现11题的选项描述含提示文字（「方向反了」「经典误解」等，变相泄题），该轮再认证据**降权**；Phase 1 关键内容改为口述抽查复盘（T1–T7：关键帧闸门/g_jpeg交接/AVFrame双通道+M-008/失败通道与时基/fMP4/重连停止），结果记后续条目；「选择题选项描述禁提示」已固化入tutor长期记忆。

- 2026-08-27（主线·1.9 口述复盘 T1–T7，应答即证据）：**T1 强通过**（解码依赖为主/容器不必然损坏；「分段=参考链重置点」自主表述）。**T2 主体通过**：写读线程归属对，行号分毫不差（web.c:197/222、cloud_ai.c:305，源码核实；自称「没细看」与引用精度矛盾=实际读过）；超纲自主产出「共享信箱 vs 订阅表」九维对比表（面试素材池）；缺口=「锁划得小/深拷贝断别名」双保证未显式分解→tutor 补讲（锁=一致性+不拖住、拷=所有权移交；只锁不拷=悬垂、只拷不锁=撕裂）。**T3 ①②强通过**（av_image_alloc 裸挂两步死/解码器池引用计数一步死/av_frame_free 内部 unref+放壳/静默泄漏风险，c:101 核实）；自评「统一判据离题」被纠正（判据正是可迁移核心）；**③ clone vs AVFrame（M-008）仍未答→tutor 补讲待复述**。**T4 强通过+自学加分**：+discardcorrupt（c:42）与 probe 5s/10MB（c:40-41）为记录外自学发现；三腿消化/计数只接写盘返回值/不对称理由/20 防抖全对（20连败@25fps≈0.8s 推演对）；措辞精修一处：「花一帧」→伤害持续到段内下个关键帧。**T5 最强**：muxer 契约/恒等 rescale（c:236/237/256 全对）/接口丢弃时基（c:155 上游知情、与 TODO② 及 rtsp 腿传 tb c:168 互证）/90 倍速+**新自推「600s 段内部时长仅 6.7s 矛盾」=变式级证据**。**T6 部分通过**：movflags 机制（位开关/+/字典 write_header 消费）对；三旗语义+提高/不保证边界未答→tutor 补讲；「冷门」判断纠正=掉电安全经典题但深度一层即可。**T7 部分通过**：重连自含理由（API 即健康检查/不拖主线程）对；stop 顺序 c:207-212 与源码逐行一致；**停止延迟未答→tutor 补讲**（标志不唤醒阻塞读、join 等本次返回、标准解=AVIOInterruptCB；本项目未注册回调且未设 stimeout=新审计观察）。三必对题（关键帧/JPEG 拷贝/主旁路）全过。T1–T7 用时未报告。

- 2026-08-27（主线·1.9 收口抽查 V1–V3）：**V1 锁迁移变式强通过**——「访问集分析法」自主构建：g_jpeg_size（写=泵线程锁内:164，读=web/ai 锁内:193/198/203+锁外:206）vs g_last_emit_us（读写=仅泵线程:141/144），访问集无交集→搬锁产生两个新临界区但 L206 裸读仍不设防、竞争原样；「锁不是模块级驱魔结界」原创句式；超额指出真正未同步对=g_cfg.target_fps（:138 无锁读 vs set_target_fps 锁内写，源码已核：规范级 data race/实践良性）＝1.7C 审计表正确迁移；越界读后果链（旧buf+新size→web send 越界）完整。**V2 段首坏 IDR 强通过（1.9 全场最佳答案）**：①写头内容来自 recorder_create 拷贝的 codecpar（:160 已核）与本包字节无关→头必然完好；②包级 KEY 信号不受位流损坏影响→切段照常走完、坏 IDR 成新段首 sample、frag_keyframe 开新 fragment、moof 标 sync sample；③后续样本结构完好语义连坐（整 GOP 到下个 IDR）；④fg=参考基座塌+预览冻结一个 GOP（g_jpeg 停旧帧，非花一帧）；⑤rtsp=start code 存亡两分支、殊途同归全体客户端丢 GOP；⑥**未要求的加分洞察：sync sample=随机访问点=seek 入口，该段门面即坏**；⑦T4 对比五维表自主产出（半径/recorder 落点/预览表现/rtsp 广度/err_count 均不动）。**V3 复述达标**（M-008 差异三轴+停止延迟因果链完整；近乎逐字复述如实记复述级，面试需换自己的话）。**判定：1.9 手册验收标准全满足**。1.9 全收口时补落八股卡三张并执行五文档同步。V1–V3 用时未报告。

- 2026-08-27（主线·1.9-3 简历条目）：**简历项目条目 v5 定稿**，落档 `面试备战/简历.md`（含「投递前自查」段：板端闸门/热更新清单/深挖锚点，标注勿粘贴进简历）。五轮迭代收敛要点：②③合并（③无独立机制，仅复述②动机；架构选型展示归开头段）；「分析并设计」改「分析并验证」＝归属修正（分工是原工程决策，「设计」接不住「设计稿在哪」，「验证」的落点=板端部署实测全功能并行，非 profiling 数字）；删「自研」（SDP/RTP/FU-A/Range 机制词承担信号，「原项目自研、我逐行梳理」挪到面试话术）；FU-A 保留（B6/B9 存货+可反讲 ts 恒 0 审计）；五条结构与 video2lcd 五五开、机制不撞车。迭代中清除的事实错误三处：源地址热更新（conf_apply_hot 五项无它，曾三次写回三次删）、「解码帧率可按负载调节」（实然=逐包解码维护参考链、节流挡缩放+编码，系统无负载反馈回路）、「按目标帧率解码」（改「按目标帧率出图」，与 1.7B 已掌握机制对齐）。**剩：8 道 why（关键帧/JPEG拷贝/主旁路隔离三题必对）+1 分钟口述版**。用时未报告。

- 2026-08-28（主线·1.9-3 why 闭卷）：**8 道 why 全部通过**（手册任务卡原题；8/8 核心因果正确，Q1/Q2/Q5 三必对全过）。亮点证据：Q2「首要问题=解码依赖、容器不必然损坏」+严格消费者（MSE/硬解）随机访问点要求＝V2 推演完整复用；Q4 反事实「提前 unref→send_packet(NULL) 触发 drain/EOF 排空→旁路静默死＋rtsp size 守卫静音」为超教学自推、drain 语义正确；Q7 换来/付出四条 trade-off（不可见性／下游不重协商 !g_fg_inited 反面／无 escalation／死锁无看护）＝面试第 4 层事故推演素材；Q8 分裂签名（切段按秒正常、文件内容时间轴坏、RTSP 腿传 tb 预览正常）复用 T5 并扩展 3600 倍速算术与修法。三处归位（非扣分）：①Q1「将 H.264 包缩小」→缩放对象是解码后像素域帧（sws_scale 640×360 再编 JPEG），压缩域/像素域术语归位；②Q2 sync 标记载体＝fMP4 的 moof/trun sample flags（stss 系非碎片化 moov 的同步表）；③Q3 default_base_moof 一句话结论留面试层。用时未报告。

- 2026-08-28（主线·1.9-3 口述版+**Phase 1 全收口**）：①1 分钟口述版通过——六句结构完整（定位/架构/双亮点/稳定性/角色钩子）、事实全对、身份口径与简历 v5 一致；三处交付前修订：原稿约 460 字超时长（润色稿约 370 字入 `面试备战/简历.md`）、「时钟选择」举例换为真缺陷「转发时间戳恒 0」、「我做的项目」→「我的项目」，另「1fps」→「节流」防口头数字被抓。②**Phase 1 正式收口**：执行分级同步第二档（Phase1手册/progress/学习掌握档案/索引/总计划五文档）+补落三张八股卡（V1 访问集法→04、V2 段首坏IDR/T5 600s矛盾→03，另修复 03 的 cleanup 占位卡漏填）。③1.9-3 用时未报告；Phase 1 确认用时维持 30.5h 口径。下一步：Phase 1.5 实现层段①。

- 2026-08-28（Phase 2/3 预制包批量预制）：应用户要求对标 Phase1.5预制包模式，新建 `Phase2_3预制包/`（README + C1–C4 概念卡 + P2-2.1/2.2/2.3A/2.3B/2.4/2.5 + P3-3.1–3.6 共 17 文件），行号全部对照 cloud_ai/event_bus/alarm/web/conf/main 当日实测（手册 08-22 版行号已漂移，以预制包 README 速查表为准，Phase 2/3 收口同步时回改手册）。README 登记**预制时新审计观察 12 条**（event_bus 快照 I/O 持锁 c:145-167 / ai interval_us 一次性 c:302 / 早退线程 running 残留 / ai stop 延迟上限 30s / on_event 无锁读配置 / g_pending 单槽合并 / web 全 send 无短写处理 / web_mjpeg_fps 作用于生产端 / handle_client recv 4096B 截断 / token 掩码不对称回写 / publish c:115 疑似残留 / parse_log_level 大写静默降级；另 P3-3.5 卡内新发现 conf_save 无并发保护），标注「开块核实后启用」。八股卡 06/07 不预写，随块增量落卡。本日另：①答用户注释核查问——Phase2/3 五模块函数注释 68/68 全覆盖、08-14 审计保持有效，无需单独重查（conf.c:195-196 注释与代码相反仍在，留动手任务）；②Phase 1 教学法复盘（四遍阅读法/弹性验收/变式抽查/铁律 21/22/24 催生/三层证据审计/M-xxx 闭环/V4L2 类比/选择题降权修正等 9 项）口头交付。C1 钩子三问已发，待学生作答。全路线索引「当前接续」已同步改 Phase 2。

- 2026-09-02（教学体系修订，未推进任务 ID）：按 C1 实战审阅落地——① agent：nvr-tutor 增铁律 26（阅读清单/L1→L3/审计主菜四条）；knowledge-tutor 明确不教 C1–C4；auditor 子任务默认不完整审计。② 预制包改顺序（2.1 先于 C2；C3/C4 并入 3.1/3.4）；C2 改为 25–30min 短块并加阅读清单；2.3A 不重复 C1；2.5/3.6 各一场。③ 预算 Phase 2 6.5h→5.5h、Phase 3 9h→5.5h，总路线约 83h→约 78.5h。**下一步仍是 2.1**。
- 2026-09-01（Phase 2·前置概念 C1 收口）：**C1 回调/发布-订阅通过（L2），3 卡入 06_云AI与事件.md**。①教学法适配：钩子三问（跨模块推理）学生反馈首读模块做不到→降级为「L1观察→L2单文件→L3跨文件」阶梯，自创「一个名字在文件间的旅行」降解法；学生另要求阅读清单先行（7 小段 15min 已写入 C1 卡）。②L1 过：event_cb_t 心智模型对，纠偏三处（typedef 用法语法/void≠void*/user 是单个 void* 不透明上下文非数组指针）；L2 过：两平行数组+锁内拷锁外调全对，纠偏「替换→拷贝（快照）」+typedef 语法二现钉死，补 MAX_SUBS 满静默。③L3「on_event 一生」时间线全对（自主带出非 none 过滤+锁外执行两个加分点；纠偏类型 vs 实例、main.c:145 调的是 alarm_init 而非 subscribe——订阅是 alarm 自己在 c:129 做的）。④6b sms 强通过：签名对契约+**先建发送线程再订阅顺序自主抓出**+「留记号踢线程」Q3 教训迁移+发布者零改动=原 Q1 通关。⑤闭卷两步（锁内快照纳秒级/解锁后调用）+三理由按严重度（防死锁-非递归锁重入/锁时长有界-契约只能约定/迭代稳定）=**V1 超额独立通过**；V2 栈帧生命周期通过（同步期有效 vs 回头读悬垂，工程正解=当场读走内容）。⑥Q2/Q3 由已有零件点破自证。**教学法沉淀（铁律25 追加）**：首读模块提问升级为导航级锚点（子问题→位置→看什么），已写入 AGENTS.md/nvr-tutor.md。用时未报告。**当时下一步写 C2；09-02 改为先 2.1，C2 后置。** 同会话另：GitHub 同步建立（工作区 git 化推 CTER970/nvr，换机先 pull 收尾 push）；Phase 1 vs 2/3 教学质量对比评估口头交付（纸面 2/3 完胜-方法论定型红利；实战验证度 Phase1 100% vs 预制包 0%，C1 钩子盲区为证）。
- 2026-09-02（主线·2.1 开块+L1 段过）：**2.1 开块，L1 段完成（阅读清单/L1 四问/纠偏/追问），L2 四问已发待答，未收口。** ①L1：Q1 启用判定（锁 g_cfg_mtx/ai_enable+token 非空/return NULL）与 Q3（interval_us 循环外 c:302 算一次）全对；Q2 动作序列漏三点补齐（c:304 NULL 出生点/c:309 continue 短路/c:314 结尾节流 usleep）；Q4 纠偏**"free 清空内存"错误心智模型**（free=归还所有权，不清零不改指针变量→悬垂之源；free(NULL) 合法、if(jpeg)=防御性写法判定对）。②**追问强通过（超出题面）**：get_jpeg 两条失败路径契约不对称（路径1 c:195 显式 *out_data=NULL / 路径2 c:198-201 malloc 失败不写输出），靠调用方 c:304 预初始化兜底；删初始化推演 double free 两分支完整（首轮 UB 栈残留→free 野指针，glibc abort 或无声堆损坏分层；二轮起 free 不置空栈槽残留→悬垂→double free）；学生原话"free 拿到的只是地址值的拷贝，根本改不了调用者的变量"。素材登记：get_jpeg 路径2 契约缺陷入面试池（2.5 可用）。③方法提炼：输出参数契约四问（成功写否/每条失败路径写否/不写时读到什么/调用方初始化否）+"删一行"思想实验。④**预算口径修正（用户质疑时间盒现实性，成立）**：09-02 新口径（30–50min/卡）未含交互往返成本（打字作答/AI 纠偏追问轮次/元问题打断），且 Phase 2 新口径零实测样本；按历史 L3 倍率 1.5–1.8× 给出现实预期——2.1 完整执行约 70–90min 纯学习（半场观察吻合），Phase 2 现实总口径约 **7.5–9h（非 5.5h）**，总路线回弹至约 81–83h；**决定：2.1 收口时按实际用时回弹校准 2.2–2.5 预算并更新本表**；卡内时间盒降级为节奏控制（防无限深挖），不再当完成承诺。⑤**学习策略再确认（用户主动问"细节全吃透 vs 框架优先+面试前迭代"）**：确认 B 模式（广度优先/塌点回补），新增**三类内容分法**——因果骨架（当场压实，抗遗忘）/证据性细节（落卡外置，忘就忘，10min 重建）/边缘细节（滤网已砍）；"会遗忘"对策=降低对当场记忆的依赖（落卡+锚点+复测+塌点回补四层机制已在）；**tutor 承诺不加非必答追问轮，用户获得"收"开关**（追问密度过高时喊停，等价"直接讲"变体）。⑥同会话元问题：Phase 2 规划核对（八单元均有独立预制卡五件套，2.3 已拆 A/B，2.4/2.5 单卡即一段）；2.1 用时未报告（半场观察约 40–55min 纯学习）。
- 2026-09-03（主线·2.1 收口）：**2.1 · AI 线程、最新 JPEG 所有权与吞吐通过**。①L2 四问：Q1 吞吐强通过（6s/张、≈0.17fps、「上限节流被实现成每次处理完额外加一秒」）；Q2 主干对+**纠偏「虚拟地址空间」术语级错误**（线程间共享同一地址空间，私有仅栈/寄存器/errno；隔离靠所有权纪律=别名隔离而非地址空间；堆共享→越界炸整栋楼）+补齐题目要的正面共享清单（g_jpeg/g_jpeg_size/g_lock 三样+进程级堆分配器/log/fd，持锁纳秒级）；Q3 方向对+**纠偏补齐第二层丢帧**（初答只说生产节流=成本闸门，漏单槽覆盖=最新值本体：5fps×6s≈30 张入槽 AI 只取最后一张）+`ai_max_queue` 幽灵字段 grep=0 证实（Q4 强通过）；②闭卷骨架八步全（小补丁：启用判定不通过出口=纯本地模式 return NULL 复述漏项）+**场景 B 时序推演亮点**：自发推演 `if(g_running)` 检查点动机（停止请求落在 5s HTTPS 期间时砍掉白睡 1s、join 提前收工）并连到 reload 开关翻转 stop→start 热重启——**踩到 2.4 主菜④（停止延迟）门槛，标前瞻不展开**；③V1 吞吐变式通过（8.2s/≈0.122fps/配置意图的 2.4%/缩水 41 倍，独立完成）；普遍现象半问由 tutor 收口（串行管道吞吐由最慢环节决定，usleep 是间隔下限非周期控制器，配置声明≠运行实然）。④4 卡入八股 06（吞吐公式/最新值两层丢帧/线程共享模型/get_jpeg 契约，累计 7 卡）。⑤**2.1 用时未报告**——Phase 2 预算回弹校准（2.2–2.5）待此数据，学生下次报告时执行。下一步=2.2（云请求六阶段/四层失败）。
- 2026-09-03（主线·2.2 开块+L1 段过）：**2.2 开块，L1 四问强通过，L2 四问已发待答，未收口。** ①前置概念已声明（base64 4/3 膨胀+JSON 文本协议限制/data URL 内联/JSON 树/curl easy 三步/Bearer 持有者令牌）。②L1 证据：六阶段管道+6 个失败 return 逐条带行号全对（248/251/256/261/266/270），「publish(277) 被层层成功条件罩住——6 return+parse==0+!=none，**只在完整证据链成立时可达**」=控制流级表述；data URL 身份三级跳（JPEG 二进制→base64 文本→伪 URL）+前缀=MIME/编码说明书+补完整树路径 messages[0].content[1].image_url.url；传输层（curl c:236，对话未完成）vs HTTP 层（4xx/5xx，话到了答案是不）区分干净；**双层解析三层结构图**（①协议JSON=信封机器保证合法/②自由文本=模型生成不保证格式/③业务JSON=prompt要求但大概率在）——比预制卡"双层"叫法更精确，面试级心智模型；小归位：OSI「网络层」措辞→传输层。③L2 四问已发：四层失败矩阵（6 return+none 归入传输/HTTP/协议结构/业务语义+附加题 b64/body malloc 失败归属）/none 过滤归属（cloud_ai c:276 vs event_bus 规则，新发布者发 none 会否入库）/enable_thinking=false 归属（OpenAI 兼容必选？提供方扩展）/五 free 配对审计（b64 c:250/body 255/resp 265/model_text 269/json_str 285；jpeg 归 ai_thread c:312 free）。④2.2 用时未报告。下一步：L2→闭卷（六阶段+每层一个失败例子）→收口。

- 2026-09-04（主线·C2 开块，未收口）：前置概念（cv=床位不存事件/谓词 g_pending/wait 原子三步/signal 即焚/虚假唤醒）+阅读清单（alarm.c 四段，行号核实：19-30/76-86/89-111/140-152）+L1 两问已发待答（Q1 线程睡在哪一行；Q2 先改哪个变量再 signal、g_pending 语义）。同会话元问题（非学习内容不入档案）：v1 套餐计费查证=prompt 次数制+工作日 14-18 时 3 倍系数、无 token 加权；新会话开场 24% 消耗与「约 6-7 轮×3 倍」吻合；对策=避开高峰时段开块/开场合并读取轮次/同段不换会话。C2 用时未报告。

- 2026-09-04（主线·2.2 L2+闭卷+收口）：**2.2 · 云请求六阶段、HTTP/JSON 四层失败通过，3 卡入八股 06（累计 10）**。①L2 四问：Q1 四层失败矩阵强通过（判据「谁定的契约」+处置映射「传输退避/HTTP 分流/结构告警不重试/语义调 prompt」；附加题超预期：b64/body malloc=第 0 层本地资源失败、write_cb realloc 失败→CURLE_WRITE_ERROR=OOM 伪装传输层需看 CURLcode、「唯二无日志静默 return」经 grep 函数边界核实成立——b64_encode/build_request_body 内部零 LOG；且能区分「这层静默」（256 !resp 外层无 LOG 但下层 c:237 已发声））。Q2 none 归属强通过（调用点约定非总线不变式、新发布者 none 直接入库推演对）+**用户自主方法论产出「规则的住所决定规则的强制力」**（中枢=不变式/调用点=约定），与 Q1 判据、③的「规范 vs 方言」同源，tutor 统一命名「找契约的主人」并入卡。③enable_thinking 归属达标；用户质疑超标，tutor 裁定：考的是归属判断非厂商协议，一句话口径封顶（符合深度滤网），补面试表达「OpenAI 兼容=事实标准非规范强制，方言字段配置化隔离」。Q4 jpeg 借用链强通过（get_jpeg 锁内 malloc 深拷贝→ai_thread 唯一 owner→c:311 借出→c:312 free；event_bus 侧 fwrite 即完+SQLITE_TRANSIENT 全绑定=借用语义的库侧实现，无路径存出指针；行号全对）——五 free 配对半边未单独交，由 Q1 六 return 枚举+Q4 链条合并覆盖，tutor 补对账表（250↔247/255↔249/260+265↔write_cb realloc/269↔264/285↔268，无泄漏无 double-free）；tutor 补观察：锁内 malloc=一次持锁读 size+分配+拷贝保快照一致性，出锁 malloc 须防 TOCTOU 得不偿失。②闭卷 8 段全链通过：六阶段+每层失败例全对（含 get_jpeg「无帧不算错误睡一周期再试」的定性），三处源码细节核实（max_tokens=300/content 双形态/strdup 序列化）+**第七条无独立 return 的失败路径自主发现**（括号配平但 JSON 非法→parse_event_json -1→else 分支 c:282-284）；一处机制精修：extract_json_object「剥围栏」实为 strchr 首个 { 天然跳过前缀（散文含 { 会错位起步）。③同会话：const 专题热身（用户主动问，编译期契约非运行时保护/四组合/UB 边界/项目锚点 event_cb_t+recorder cast away const；属八股地图专题 8，未落卡留收尾统一）；GitHub 双向同步落地（本地拉齐 8f3b6ee、agent/教学体系入库，本会话产出未 commit 留用户决定）。④2.2 用时未报告（L2+闭卷两段）。下一步=C2 短块。

- 2026-09-06（主线·C2 收口）：**C2 · 条件变量：等待/通知通过（L2），2 卡入八股 06（累计 12）**。①L1 两问：Q1 全对（c:96 真睡眠+内核摘下挂 g_cond 等待队列+轮询 vs 等待分水岭对比）；Q2 顺序（81 先写谓词→82 按铃，锁内）/g_pending 三层语义（载荷秒数+0=无事+最新值覆盖，兜底 3 防存 0 静默消失）全对+主动复用 2.1 单槽语义；tutor 精修一处：反序「醒来查 0 睡回去」交错在「都在锁内」时被锁挡住（消费者查谓词必先拿锁、signal+write 同一临界区），真正敞开窗口=signal 或写谓词至少一个在锁外——为 V2 埋柱；另：g_active_s 负数也是静默消失路径（谓词==0 拦不住，落 101 continue）；「进行中来事件=覆盖」精化=响铃中槽已清空、新事件进空槽响完再补一轮，真覆盖只在未取走窗口（留 2.3B）。②填空时序图（图画不了改骨架填空版）：5 空+追问全对（放锁/入睡/醒来抢锁/返回必持锁/T3 锁无主即拿即走），亮点=「放锁与入睡不原子→通知在挂上队列前响过→睡死」+「消费者等铃、生产者等锁互为死锁对」反证独立构造；措辞精修：signal 只作用于已挂队列的线程（「响过没被接」≠「得到通知后入睡」）。③图末两问：①锁外 sleep 主干对+补串线（on_event 跑 publish 线程=AI 线程，持锁睡 5s 拖整条流水线，次生 reload/stop；锁粒度原则）；②强通过=「事件存在 publish 线程调用栈上、on_event 还没返回」（C1 V2 栈帧迁移，不丢、解锁后进 T4）。④找茬两版：坏版本一（无锁+先铃后写）两病拆分（数据竞争+丢失唤醒）+时序+表现+修法全对；坏版本二（if 代 while）病名+时序+「本代码靠外层 while 补考+101 continue 双兜底侥幸」自主判定+金句「cond_wait 返回=该重新看一眼条件≠条件成立」；学生自报初未想到虚假唤醒（不影响判定）。⑤闭卷：骨架+谓词全式+四句为什么全对（①封死「查完 0→入睡」的缝 ②g_pending 业务条件/g_running 停机通道 ③取走清零锁内=防清零覆盖新触发 ④锁外 sleep）；**V2 独立构造**「无锁查谓词见 0→生产者写 5+signal（队列空铃蒸发）→消费者才躺进 wait→终态 g_pending=5 永睡」+靶心总结（锁内查谓词+原子放锁入睡=窗口焊死）。⑥C2 用时未报告。下一步=2.3A（event_bus 发布链，C1 内容不重讲）。

- 2026-09-07（主线暂停+速讲轨道开建，未推进任务 ID）：①2.3A 开块状态登记（09-06/09-07 两轮，详见「当前位置」09-07 条目）：任务卡两段表+L1 三问已发待答，SQLite 前置直讲两轮。②用户拍板：秋招投递优先，主线打磨暂停，投递后口令「继续 2.3A」从 L1 待答接续。③新增旁路 agent `nvr-quick-tour`（项目速讲教练；交付五件=电梯版/白板架构图/我做了什么/追问应答卡/短板话术；归属诚实原则沿用简历 v5，产出落 `面试备战/项目速讲/`），AGENTS.md 坐标系登记。④到期复测继续暂缓累积登记（约 55 张，收尾统一处理）。⑤同日速讲轨道审查+说明落档：对 nvr-quick-tour 补强三处（铁律1数据流主轴显式化/课程2增量重心=未学模块/「能力边界与预期」小节固化六层断点），轨道基线说明落 `面试备战/项目速讲/00_速讲轨道说明.md`。⑥同日二次落地(用户确认建议后执行):口令路由写入 AGENTS.md/nvr-tutor 开场第一步;progress「待开块」与旁路段粘主线材料两处自相矛盾已修;现成讲义 01/02/03 落盘(03 含板端降级闸门);全路线索引与面试备战 00 索引已改接到 2.3A 暂停+速讲旁路。

## 下次从这里继续

**当前接续：Phase 3 已于 2026-09-20 全收口（阶段总验收+五文档同步+八股 07 共 20 卡+git commit），主线模块精读（Phase 0/1/2/3）全部完成。下一专场由用户定：①口令「简历完善」——v5 基础上迭代技术点/战绩（新弹药=Phase 3 收口句+两发实然推翻+validate_conf 动手故事+#26+90s 阶段复述；投递闸门 Phase2/3✅、板端跑通❌）；②口令「继续 Phase 5」——V821 概念一天带过；③ Phase 6 面试冲刺。**

Phase 3 顺序（已走完，09-20 闭合）：3.1 ✅（09-17，C4 并入核对）→ 3.2 ✅（09-17，MJPEG）→ 3.3 ✅（09-18，Range）→ 3.4 ✅（09-19，socket 生命周期+C3）→ 3.5 ✅（09-20，conf 原子保存+热更新）→ 3.6 ✅（09-20，AI 代做+阶段总验收）。用时均未报告（回弹待报告）。

挂起清单：validate_conf 板端验证（静态完成≠已验证，三条 curl 建议已交付）；到期复测累计约 62+20 张（八股 07 新增）继续暂缓至收尾；agent 三文件改动（铁律 29 出处）未 commit 待入库；台账#4 与铁律 29 冲突备注待用户拍板。

**速讲轨道已终结（2026-09-14，8/9 段过闸、段 9 取消）**：agent 与口令路由已移除；产出文件留存「面试备战/项目速讲/」（讲义 01–05+过码台账+复述卡点清单）——04 口述稿/05 应答卡=面试弹药，过码台账=主线 L1 折算事实源。

Phase 2 顺序（已走完，09-16 闭合）：C1 ✅ → 2.1 ✅ → 2.2 ✅ → C2 ✅ → 2.3A ✅ → 2.3B ✅ → 2.4 ✅ → 2.5 ✅。用时坐标≈9.4/9.5h（各单元未报告）。

Phase 1.5 实现层+审计仍在**定向回补池**（Phase 3 窗口已结束，可排期；触发=临近面试/模拟面试塌点）。投递最硬闸门仍是板端跑通+D1–D6。
